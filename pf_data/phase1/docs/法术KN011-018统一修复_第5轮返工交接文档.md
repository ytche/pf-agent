# 法术 KN011~KN018 统一修复 — 第 5 轮返工交接文档

> 撰写：k3（方案设计方 + 审计方），2026-07-30
> 执行：执行模型（整包交付，护栏内自主决策）
> 返工依据：[第 4 轮审计报告](法术KN011-018统一修复_审计报告_k3_第4轮.md)（commit `299343a`）
> 基线 commit：`299343a`（第 4 轮审计提交），branch `fix/kn011-018-unified`

---

## 0. 当前状态一句话

第 4 轮 T1/T2 已通过审计（MA 链接 253/253、后处理链固化、4 处文档更正），**本轮 U1~U4 全部是文档级更正**——豁免清单 18 行定性翻牌、KN020 规模重登记、KN019 D 类补登 4 条。**不动解析器、不动 chunks.jsonl、4200 不变、pytest 不得掉。**

k3 已把所有需要判断的内容全部预置（翻牌清单、37 条 KN020 ① id、4 条 D 类登记行、四簇判定规则），执行方**照单更正 + 复算核对**即可，不需要重新定性。

---

## 1. 返工项总览

| # | 内容 | 改动文件 | 预估 |
|---|---|---|---|
| U1 | 豁免清单 18 行 ✅→❌ 翻牌 + 4 张汇总行重算 + 箭头标签统一 | `docs/法术KN011-018统一修复_第3轮交付审计文档.md` §9.1附 | 小 |
| U2 | KN020 ① 规模 8→37（全量 id 清单）、unique chunks ~85→**114**、field-values 91→**120** | `docs/已知问题记录.md` KN020 章节 | 小 |
| U3 | KN020 簇口径统一为 k3 四簇（①37 正交 / ②42 / ③36 / ④7），清除"登记 8 实样列 10"等自相矛盾 | 同上 | 小 |
| U4 | KN019 D 类补登 4 条、D=23、总数 ~72 重算、4 个枚举标题 chunk 补裁定 | `docs/已知问题记录.md` KN019 章节 | 小 |

---

## 2. U1：豁免清单定性更正

改动位置：`docs/法术KN011-018统一修复_第3轮交付审计文档.md` §9.1附（第 4 轮 T3 补全处）。

### 2.1 判定标准（先立标准，再翻牌）

持续时间类字段的合法值**到持续时间表达式为止**（如 `1小时/等级`、`1轮/等级 (可解消)`、`专注，最多每等级1分钟。`）。表达式之后出现的任何"你/该法术/动物/本法术…"开头的句子**都是正文散文**，无论中间有无 `**`、有无空格、源数据是否同段——一律真吞。"源数据合法"只适用于值本身就是游戏术语描述的情形（墙类尺寸枚举、多目标约束等），不适用于"字段表达式+叙事句"拼接。

唯一 ✅ 保留：`spell_page_1150_0000`（`'专注，最多每等级1分钟。'`——完整的替代表述式时长，无散文）。

### 2.2 C 表（duration 含 `。`，26 行）：16 行翻牌

以下 16 行 ✅→❌（→ KN020 ①），k3 已逐条核验实际值：

| chunk_id | 实际值形态（k3 核验） |
|---|---|
| `spell_Spell UI_0087` | `1小时/等级 或 直至耗散` + 散文"你的喉咙中生长出1个特殊腺体…" |
| `spell_Spell UI_0097` | `1轮` + 散文"你在两扇门…之间设立起1道魔法通道…" |
| `spell_page_1219_0000` | `1小时/等级（可解消）` + 散文"动物会认为你是一个社交优越者。" |
| `spell_page_1219_0017` | `1轮/等级` + 散文"当你冲锋时…" |
| `spell_page_1219_0018` | `专注，最长1小时/等级` + 散文"这个法术可以让你根据足迹…" |
| `spell_page_1219_0043` | `1轮/等级` + 散文"你的翅膀长出了有毒的荆刺…" |
| `spell_page_1219_0044` | `1轮/等级或直到你移动` + 散文"你的翅膀会变成一面木制屏障…" |
| `spell_夜之血脉BotN_法术_0000` | `专注 \n+ 1d4轮` + 散文"本法术如同弱效幻影…" |
| `spell_夜之血脉BotN_法术_0004` | `1日/CL` + 散文"本法术和盗取青春效果相同…" |
| `spell_异能选集PA_法术_0004` | `1天或直到发散（见描述）` + 散文"你扩展你的意志…" |
| `spell_page_1104_0004` | `1小时/等级` + 散文"你可以快速吸收目标文本中的知识…" |
| `spell_page_1104_0005` | `立即` + 散文"你吞下一剂毒药…" |
| `spell_page_1104_0006` | `1小时/等级` + 散文"当你处于一些预言法术的边缘时…" |
| `spell_page_1104_0010` | `1天/等级` + 散文"这种法术如同诱骗人类…" |
| `spell_page_1104_0013` | `10分钟/等级` + 散文"你发誓不透露自己的身份…" |
| `spell_page_1104_0023` | `1分钟/等级` + 散文"当面对一个重要情况时…" |

原有 9 行 ❌（OA×3、UI_0022、UI_0040、MO×3、page_1365_0103）定性不变，但**箭头标签统一改为 → KN020 ①**（原标 ③ 错误，见 U3 四簇定义）。

**C 表汇总行更正为**：合法 **1** + 真吞 **25**（原"合法 16 + 真吞 10"，且表内 17✅+9❌ 与汇总行自相矛盾一并消除）。

### 2.3 B 表（target len>40，16 行）：1 行翻牌

- `spell_page_1104_0012` ✅→❌（→ KN020 ①）：实际值 = `'距离内任何数目的生物或物体` + 散文"被设计来保护大量物品免于被奈克斯弧光领主的竞争对手发现，除了如上所述，这种法术功能如同回避侦测。"`。其余 15 行 ✅ 不变。

**B 表汇总行更正为**：合法 **15** + 真吞 **1**（原"全部 16 个均为合法长值"）。

### 2.4 D 表（duration len>40，19 行）：全部真吞

`page_1150_0000` 仅 12 字符，**不在** len>40 集合内（k3 复算：19 = 17 与 C 重叠 + `page_1219_0025` + `page_1365_0315`）。17 个重叠行随 C 表翻牌，`page_1219_0025`（`'1小时/等级` + 散文"法术如同变形者的恩赐…"`）✅→❌，`page_1365_0315` 维持 ❌。

**D 表汇总行更正为**：合法 **0** + 真吞 **19**（原"合法 9 + 真吞 10"）。

### 2.5 A 表（effect len>40，28 行）：定性不变，箭头统一

19 合法 + 8 真吞 + 1 MA 的定性**维持不变**（k3 审计已认定可辩护）。仅将 8 行真吞的箭头统一为 → KN020 ①（含 `UM_0020`，原标 ③；其值无 `**`，属"散文真吞"而非形态簇）。

### 2.6 §9.1附 总结表（整表替换）

| 指标 | 总计 | 合法长值（豁免） | 真吞正文（→ KN020 ①） | MA 口径 |
|---|---:|---:|---:|---:|
| effect len>40 | 28 | 19 | 8 | 1 |
| target len>40 | 16 | 15 | 1 | 0 |
| duration 含 `。` | 26 | 1 | 25 | 0 |
| duration len>40 | 19 | 0 | 19 | 0 |

---

## 3. U2：KN020 ① 规模更正（37 条全量清单）

`docs/已知问题记录.md` KN020 章节，① 行与实样段整体替换。

### 3.1 ① 新定义

**字段值 = 合法字段表达式 + 正文散文粘连**（无论有无 `**` 分隔、有无换行）。按"值含散文"判定，与 ②③④（按 `**` 残留形态判定）**正交**——一个 chunk 可同时属 ① 和 ③（如 `OA_0043`）。

### 3.2 ① 全量清单（37 chunks，k3 逐条核验，直接采用）

**duration 真吞（27）**：

```
spell_MO-神话源始_0006 / 0010 / 0014
spell_Spell OA_0043 / 0045 / 0115
spell_Spell UI_0022 / 0040 / 0087 / 0097
spell_page_1104_0004 / 0005 / 0006 / 0010 / 0013 / 0023
spell_page_1219_0000 / 0017 / 0018 / 0025 / 0043 / 0044
spell_page_1365_0103 / 0315
spell_夜之血脉BotN_法术_0000 / 0004
spell_异能选集PA_法术_0004
```

**casting_time 真吞（1）**：`spell_page_1365_0482`（`'为1分钟或更长的法术，` + 散文"作祟灵同样会变得具有破坏性…"`）

**target 真吞（1）**：`spell_page_1104_0012`

**effect 真吞（8）**：

```
spell_Spell UM_0020        （'－近战：如果你们同时攻击…' 散文片段）
spell_page_1494_0005       （豁免描述文）
spell_盗贼议会_0000         （flavor text）
spell_动物档案AArch_法术_0000 / spell_Spell AArch_0000（同源重复）
spell_page_1151_0005 / 1153_0000 / spell_page_1365_0136
```

### 3.3 KN020 规模数字（整表替换）

| 维度 | 旧值 | **更正为** |
|---|---:|---:|
| ① 真吞散文 | 8 | **37**（duration 27 + casting_time 1 + target 1 + effect 8） |
| Unique 脏 chunks | ~85 | **114**（M1 85 ∪ ①；① 中 29 个无 `**` 粘连不在 M1 口径内，8 个与 M1 重叠） |
| 全部脏 field-values | 91 | **120**（91 + 29） |
| ① 与 ②③④ 重叠 | — | 8（OA×3、MO×3、page_1365_0315、page_1365_0482——带 `**` 的粘连真吞） |

删除原"① 8 chunks"表述与其实样清单（自身列 10 项与计数矛盾），以上述全量清单替换。

---

## 4. U3：KN020 簇口径统一（四簇，穷尽且互斥）

KN020 章节的三簇表替换为下表。②③④ 按 `**` 残留形态划分，k3 已验证：**42+36+7 = 85，穷尽 M1 脏集合、chunk 级互不重叠、无未归类**。① 与三者正交（§3.1）。

| 形态簇 | 规模 | 判定规则（可机检，按优先级 ②→④→③ 归簇） | 典型实样 |
|---|---:|---|---|
| ② 悬挂 `**` | 42 | 值以 `**` 开头，或任一行以 `**` 结尾（截断后的孤星号/包裹残留） | `OA_0084` `'10分钟/等级 (可解消)**'`；`UM_0179` `'立即 **\n或 1轮'` |
| ④ 换行后行首 `**` | 7 | 不满足 ②，且第 2 行及以后存在以 `**` 开头的行（跨行字段边界残留） | `page_1219_0029` area `'…任何灰尘…\n**1轮/等级 (可消解)'`；`page_1494_0012` range `'…5尺）\n**一头被召唤来的元素…'` |
| ③ 行内粘连 | 36 | 不满足 ②④，行中部含 `**`（无换行分隔的 `**标签**` 粘连，含带 `**` 的真吞） | `AG-冒险者指南_0017` `'近距 (25尺+5尺/2级)**区域：** …'`；`OA_0043`（兼属 ①） |
| ① 真吞散文（正交） | 37 | 值 = 字段表达式 + 散文粘连（§3） | `UI_0087` `'1小时/等级 或 直至耗散你的喉咙中生长出…'` |

配套更正：

1. 删除 `UC_0031` 在 ②③ 实样中双列的错误（按规则它属 ②：`'1轮/等级或 **'` 行尾 `**`）；
2. 修复优先级行维持"② > ① > ③"或执行方重排均可，但须与四簇共存不矛盾；
3. ④ 的修复方向注明：与 Bug A 的 `\n` 分支同源，需把"行首 `**` 截断"从只查 lines[1] 扩展到全行扫描（本轮只登记）。

---

## 5. U4：KN019 D 类补登 + 总数重算

`docs/已知问题记录.md` KN019 章节。

### 5.1 D 类补登 4 行（k3 已逐条核验存在性与标题，直接采用）

| chunk_id | 来源书 | title（正文片段） | 对应法术 |
|---|---|---|---|
| `spell_page_1365_0188` | COMP | `，除了你可以召唤1个小巨灵 (janni)…` | 召唤巨灵，次级 续段（与已登记 0191 同法术） |
| `spell_Spell CRB_0379` | CRB | `(探知) 效果对目标物品的定位，如探知…` | 遮蔽物品 续段（前块 CRB_0378 = stat block） |
| `spell_Spell CRB_0428` | CRB | `(possess) 或精神控制…` | 防护邪恶 续段（前块 CRB_0427 = stat block） |
| `spell_Spell APG_0218` | APG | `(如果原本就是疲乏状态，那么进入力竭…` | 燃烧之风 续段（前块 APG_0217） |

共同特征：标题以 `，`/`(` 开头——前三轮扫查启发式（散文主语开头）的盲区，补登时在 D 类形态描述中加一句"含以括号/逗号开头的正文片段标题"。4 条均经 k3 在第 1 轮备份（4470）中按内容验证已存在（非回归），与既有 19 条同性质。

### 5.2 计数与规模数字更正

- D 类表当前写"20（CRB 9 + UM 8 + APG 1 + COMP 2）"但实际只列 19 行（CRB 8）——补登后统一为 **D = 23（CRB 10 + UM 8 + APG 2 + COMP 3）**，表头、规模数字表、问题描述段三处对齐；
- KN019 总数：**~72**（44 A + 3 B + 2 C + 23 D），若 page_625_0022 裁定保留则 C=1、总数 ~71——两种口径选一种写明，另一处加注；
- 占比：72 / 4200 ≈ **1.7%**。

### 5.3 枚举标题 chunk 补裁定（4 条）

独立扫查另发现 4 个与已裁定边界候选（`APG_0042`/`APG_0242`）同形态的枚举标题 chunk，按同一口径裁定并写入边界候选裁定区：

| chunk_id | title | 裁定（k3 建议） |
|---|---|---|
| `spell_Spell APG_0067` / `APG_0099` / `APG_0166` | `(Miracle)，移除诅咒` | ✅ 合法枚举标题（神迹术法术列表中段），加"首部中文名缺失"格式脏注记 |
| `spell_Spell APG_0223` | `(pulley)，门栓` | ✅ 合法枚举标题（固化音符功能枚举中段），同上注记 |

裁定理由：与 `APG_0042`（破除结界、复原术枚举）同根因（枚举列表中段被 split），不计入 KN019 各子类总数，与 `page_805_0007` 的"标题格式脏"注记同类处理。

---

## 6. 红线（护栏）

1. **不动解析器代码、不动 chunks.jsonl、不动 verify 脚本**：U1~U4 全部只改 `docs/法术KN011-018统一修复_第3轮交付审计文档.md` 与 `docs/已知问题记录.md`。
2. **chunk 总数 4200、pytest 264 不得变化**（本轮无代码改动，跑一次确认即可）。
3. **照单更正，禁止重新定性**：U1 的 18 行翻牌、U2 的 37 条清单、U4 的 4 条补登均为 k3 逐条核验的终值；执行方若对某条有异议，**列出来单独讨论，不得静默改回**。
4. **所有数字可复算**：交付文档附复算命令（§7 已给）；四簇计数必须用 §4 的判定规则脚本跑出 42/36/7，跑不出就停下来查规则实现，不得手填。
5. **消除自相矛盾**：改完后全文 grep 检查旧数字（`8 chunks`、`~85`、`合法 16`、`~69`、`20（CRB 9`）无残留。
6. **Git 安全**：提交前 `git status` 确认只动上述 2 个文件。

---

## 7. 验收口径与复跑入口

### 7.1 交付要求

第 5 轮交付文档（可简）：U1~U4 逐项给改前/改后对照 + 复算命令输出；明确声明"18 行翻牌、37 条清单、4 条补登全部照单执行"或列出异议项。

### 7.2 复算命令（k3 第 5 轮审计将原样重跑）

```bash
cd /Users/chezi/code/java/pf_agent/pf_data/phase1

# U1/U2 复算①: duration 含。真值（期望 26 条，合法仅 page_1150_0000）
python3 -c "
import json
for l in open('vectorizer/output/法术/chunks.jsonl',encoding='utf-8'):
    c=json.loads(l); d=(c.get('metadata') or {}).get('duration')
    if isinstance(d,str) and '。' in d: print(c['chunk_id'],'|',repr(d[:80]))"

# U1 复算②: duration len>40 不含 page_1150（期望 19 条全真吞）
python3 -c "
import json
for l in open('vectorizer/output/法术/chunks.jsonl',encoding='utf-8'):
    c=json.loads(l); d=(c.get('metadata') or {}).get('duration')
    if isinstance(d,str) and len(d)>40: print(c['chunk_id'],'|',repr(d[:80]))"

# U3 复算: 四簇计数（期望 ②42 / ④7 / ③36，无未归类）
python3 << 'EOF'
import json
chunks={c['chunk_id']:c for c in (json.loads(l) for l in open('vectorizer/output/法术/chunks.jsonl',encoding='utf-8') if l.strip())}
F=('duration','target','effect','school','casting_time','range','area','spell_level')
def m1(c):
    m=c.get('metadata') or {}
    return sum(v.count('**') for f in F for v in [m.get(f)] if isinstance(v,str))
dirty={cid for cid in chunks if m1(chunks[cid])>0}
def classify(v):
    lines=v.split('\n')
    if v.startswith('**') or any(l.rstrip().endswith('**') for l in lines): return '②'
    if any(l.strip().startswith('**') for l in lines[1:]): return '④'
    return '③'
from collections import Counter
cnt=Counter()
for cid in dirty:
    m=chunks[cid].get('metadata') or {}
    cls={classify(m[f]) for f in F if isinstance(m.get(f),str) and '**' in m[f]}
    cnt['+'.join(sorted(cls))]+=1
print('M1:',len(dirty),dict(cnt))
EOF

# U2 复算: KN020 unique（期望 114 = 85 ∪ 37条①清单）
# （① 清单见 §3.2，脚本从略——将 §3.2 的 37 个 id 与 M1 集合并集计数即可）

# 全局不变量
python3 -m pytest vectorizer/tests/ -q   # 264
wc -l vectorizer/output/法术/chunks.jsonl  # 4200
```

### 7.3 k3 第 5 轮审计范围

只复核 U1~U4（翻牌正确性抽查、37 条清单与登记一致性、四簇计数复跑、D 类 23 条与总数核对、旧数字残留 grep），**不再全量重算**已通过的 T1/T2。
