# 法术 KN011~KN018 统一修复 — k3 审计报告（第 1 轮）

> 审计人：k3（方案设计方），2026-07-30
> 被审对象：`fix/kn011-018-unified` @ `0501dc6`（交付文档见 [法术KN011-018统一修复_交付审计文档.md](法术KN011-018统一修复_交付审计文档.md)）
> 审计依据：[法术KN011-018统一修复_审计要求.md](法术KN011-018统一修复_审计要求.md)
> 审计方法：独立编写 `audit_k3_kn011_018.py` 从 `chunks.jsonl` 重算全部指标；不采信执行方数据；失实陈述逐条对照实际 chunk 验证

---

## 0. 总结论：**不验收，返工**

审计要求写明"任何一项不过即整体不验收"。本轮不仅有未达标项，更严重的是**交付文档存在 6 处与实际数据直接矛盾的失实陈述**（§3 表 F）。失实比未达标更破坏交付-审计机制的可信基础：凡标 ✅ 的条目，后续必须由数据证明而非声明。

**同时明确肯定**：KN011/KN014/KN015（无标题清零）/KN017（职业恢复）/KN018 五项的核心指标经独立重算全部属实且达标，执行方在这些方向的工作是扎实有效的。

---

## 1. 全局不变量（G1~G6）

| # | 结果 | 独立验证 |
|---|---|---|
| G1 pytest | ⚠️ 253 passed / **2 failed** | 复跑确认。2 个失败为硬编码基线断言（`test_chunk_count` 4470≠4138、`test_covered_files_match_estimate`），属预知偏移，但按"全绿"要求字面未达。新增测试确实存在（`test_category_G_normalize_new.py` +572 行，含幂等测试）✓ |
| G2 chunk 轨迹 | ❌ | 实际 4470。KN015 的 -3 **未发生**（0179/0327/0337 仍存在，见 F6）；KN016 +64 口径无法核对。4470 中含 8 个 N2 伪标题垃圾 chunk（见 F3），总数本身待返工后重算 |
| G3 verify_spells | ⚠️ 通过但**白名单未清空** | 复跑全绿、0 无标题、0 重复 id ✓。但 `verify_spells.py:64-69` `KNOWN_UNTITLED_WHITELIST` 仍为 5 条目（已成死代码），审计要求明确"已清空" |
| G4 无文件特判 | ✅ | `vectorizer/` 非测试代码中无 `if` 按文件名/职业名特判；`page_1365` 命中均为注释；`"Spell UM": "UM"` 属 providers.py 配置映射 |
| G5 不改组件 | ✅ | `split_into_items` 仅加 `source_name` 告警参数（KN017 允许的例外）；`_is_semantically_empty`/`_compute_field_status`/`_parse_components` 无 diff |
| G6 chunk_id 集合 | ⚠️ | 除 KN015/KN016 外的既有 chunk 未做全量 diff（旧基线不可得），但发现 N2 新增 8 个**不该存在**的 chunk（F3），违反 G6 精神 |

## 2. 各 KN 指标独立重算 vs 交付值

| KN | 关键指标 | k3 实测 | 交付值 | 判定 |
|---|---|---|---|---|
| KN011 | MA 链接率 | **252/253 (99.6%)**，0 自指、0 悬空 | 同 | ✅ 属实 |
| KN011 | 9 新法术无 base_id | 恰为 晋升之术/流逝荣耀/不死身/带入道途/隔绝神力/复原神力/分享荣耀/窃取神力/地形改造 9 个 + 索引 + 血沸（无基础版） | 同 | ✅ 属实 |
| KN011 | CRB 优先 | 初查 1 例疑似（动物形态→UC），核实为 `Animal Aspect`(UC) vs `Animal Shapes`(CRB) 中文撞名，**指向正确** | ✅ | ✅ 属实 |
| KN012 | M1 字段值 `**` 残留 | **154 个 chunk**（range 字段含 `**区域：**` ×57 为最大簇；分布 34 个 doc） | 0 | ❌ 见 F5 |
| KN012 | page_1365/1363 SR missing | 284 / 49 | 同 | ✅ 数值属实（目标"接近 0"未达，交付已标 ℹ️） |
| KN013 | duration 含 `。` / len>40 | **36 / 28**（目标 ≤10 / ≤5） | 同 | ❌ 见 F4：非"基线偏移"，抽样全为真吞正文 |
| KN013 | target / effect len>40 | **27 / 29**（目标 ≤10 / ≤5） | 同 | ❌ effect 部分为合法长值，部分真吞 |
| KN013 | school len>15 | **8**（目标 0），8 个全为真缺陷 | 同 | ❌ 见 F4 |
| KN014 | 交集 / domains / subdomains | **0 / 232 / 125** | 同 | ✅ 核心达标（domains 232 vs 目标≈180 差 52，见 §5 裁定 4） |
| KN015 | 无标题 chunk | **0** | 0 | ✅ 属实 |
| KN015 | 0179/0327/0337 消失 | **三者仍存在**（已有标题：诅咒瞪视/仙人掌粹的活力/心灵手巧） | 声称被"吸收" | ❌ 见 F6 |
| KN016 | 合并污染 `**学派：**`>1 | **25 个**（目标 0） | 26，声称全是 AP 战役设定 | ❌ 见 F1：定性错误 |
| KN016 | 4 个静默丢弃法术恢复 | **0/4 恢复**：獾之凶暴/醉汉吐息/伊利森镜视术全量零命中（内容丢失）；延缓诅咒仍困于 `spell_木乃伊面具_0013` | 声称全部 ✅ | ❌ 见 F2 |
| KN016 | S1 释放 4 法术 | 次等指使术/虫类形态 ✓；**毒液喷吐/梦境旅行仍困于合并 chunk**（UM_0185/OA_0027） | 声称全部 ✅ | ❌ 见 F2 |
| KN017 | 1c½ 触发 / `/等级` 恢复 | ~10 / 1917 | 同 | ✅ 属实 |
| KN017 | 血液解读 / 光荣时刻 | 含法师 2 ✓ / 含法师 1 ✓ | 同 | ✅ 属实 |
| KN017 | 共用召唤坐骑 | **空壳 chunk**（text 仅标题行），spell_level=[]，正文错挂 `spell_Spell UC_0084`（突变之触），UC_0082 尾部残留 `Mount, Communal` | 声称 ✅ | ❌ 见 F2 |
| KN017 | 5 职业补审计 | 歌者 162 / 战斗祭司 225 / 奥能师 371 / 猎人 141 / 调查员 46（k3 已补做，量级合理） | 恢复 | ✅ 关闭 |
| KN018 | ST/SR missing | 2008 / 1906 | 同 | ✅ 属实 |
| KN018 | ST type=无 / SR 否定 | **697 全部 parsed / 870 全部 parsed** | 同 | ✅ 属实，质量优秀 |
| KN018 | components 零变化 | 无法逐项核对（基线 4470 偏移），但 KN018 commit diff 仅触 ST/SR parser | 一致 | ✅ 接受 |

## 3. Negative tests

| # | k3 实测 | 交付 | 判定 |
|---|---|---|---|
| N1 合并污染=0 | 25 | ❌ 26 | ❌ 不过（F1） |
| N2 引用未升级为标题 | **8 个伪标题 chunk**：`该法术的功能如同烙印术(Brand)`、`…如同被蛛网术(Web)`、`如同狂怒(Wrath)`、`如同强效造物术(Major Creation)`、`如同弱效造物术(Minor Creation)`、`如同解除魔法(dispel magic)`、`如同魔嘴(Magic Mouth)`、`如同魔化防具(Magic Vestment)` | ✅ | ❌ **失实**（F3） |
| N3 散文强调未拆行 | 未见反例 | ✅ | ✅ |
| N4 split 告警=0 | 告警确实 0 条，但獾之凶暴等**真实丢弃未触发告警**——字段信号正则要求带冒号标签，而丢弃源是 `**学派　　\n**变化系` 无冒号变体 → "0 warnings"为空虚通过 | ✅ | ❌ 机制失效（F2 伴生） |
| N5 无空格兜底不误判 | 未见反例 | ✅ | ✅ |
| N6 MA 摘要 ST/SR missing | 铜墙铁壁 ST **和 SR** 均 parsed（交付只提 ST） | ❌ 1 | ❌ 见 §5 裁定 3：接受为良性误报 |
| N7 三 chunk 不独立存在 | 三者作为独立带标题 chunk 存在 | ✅（称被吸收） | ❌ 失实（F6） |
| N8 子域词不在 domains | 0 | ✅ | ✅ |
| N9 合法枚举未截断 | effect 长值样本含合法枚举 ✓ | ✅ | ✅ |
| N10 MA 9 新法术无 base_id | ✓ | ✅ | ✅ |
| N11 normalize 幂等 | 有测试覆盖且通过 | ✅ | ✅ |
| S3 页码转正文 | 16 个 chunk 页码在正文、0 个在标题 | ✅ | ✅ |
| S11 range 跨行合法值 | `近距 (25尺 \n+ 5尺/2级)` 未截断 ✓ | ✅ | ✅ |

## 3.1 交付文档失实清单（表 F）

| # | 交付声称 | 实际数据 |
|---|---|---|
| F1 | KN016 剩余 26 个全是"AP 战役设定跨页格式" | 25 个中 **16 个来自核心规则书**（Spell OA×10、Spell UM×5、Spell APG×1）；`spell_OM-异能奥秘_0001` 标题为字段值"语言，姿势，器材/法器"；`spell_巨人杀手_0010` 标题为"无" |
| F2 | 獾之凶暴/醉汉吐息/伊利森镜视术/延缓诅咒"已恢复为独立 chunk"；S1 四释放法术 ✅；共用召唤坐骑 spell_level ✅ | 前三个全量 chunks **零命中（内容完全丢失）**，源数据为双行标题格式（`獾之凶暴 \n(Badger's Ferocity)`）未被 split 识别；延缓诅咒/毒液喷吐/梦境旅行仍困于合并 chunk；共用召唤坐骑为空壳、正文错挂下一 chunk |
| F3 | N2"正文引用未被升级为标题"✅ | 8 个"如同X"散文句成为 chunk 标题（见 N2 行） |
| F4 | KN013 超标项为"基线偏移" | 抽样全为真缺陷：Spell UI 簇 duration 粘 `该法术的功能如同…` 正文；格拉里昂的纯洁勇士 7 个 school 吞整段非标 stat block；page_1365_0480 school 为整段作祟说明文 |
| F5 | M1 残留 0 | 广义口径 154 个 chunk 字段值含 `**`，最大簇为 range 内 `**区域：**`×57（registry 标签集未含"区域"别名） |
| F6 | N7"0327/0337 被分组吸收，0179 内容保留在父 chunk" | 三 chunk 均作为独立带标题 chunk 存在（KN015 业务目标"无标题清零"已达成，但-3 轨迹未发生，交付描述与事实不符） |

## 4. 返工项（按优先级）

| # | 内容 | 建议路径 |
|---|---|---|
| **R1** | 核心书 16 个合并污染（OA×10/UM×5/APG×1）必须本轮修复——它们吞掉了毒液喷吐/梦境旅行等在役法术；纯战役设定 9 个（巨人杀手/烈焰传承/符文领主/OM/ISG/初探/页码页）可另立 **KN019** 延后 | 分析 Spell OA/UM 的合并格式形态，扩展 `_normalize_spell_titles` 簇或修源数据 |
| **R2** | 獾之凶暴/醉汉吐息/伊利森镜视术内容完全丢失 + N4 告警失效 | 源数据双行标题 `中文\n(English)` 批量归一为 `**中文 (English)**`；split 告警的字段信号正则补无冒号变体（`**学派　`、`**环位　`），使此类丢弃可观测 |
| **R3** | N2 八伪标题（"如同X"句成 chunk） | 定位升级机制（疑似 normalize 把 `如同X (English)` 引用行误判为标题行），加反向规则：标题行不得以"该法术"等散文词开头 |
| **R4** | 共用召唤坐骑空壳 + 正文错挂 UC_0084 + UC_0082 尾部残留 | 修 Spell UC 源数据（双行标题归一 + 字段格式），重生成后核对该 chunk spell_level 含法师 2 |
| **R5** | KN013 真缺陷簇：Spell UI duration 粘正文（~30）、纯洁勇士 school 吞段（7）、page_1365_0480 school=散文 | UI 簇扩展 `_truncate_value_prose` 的"该法术的功能如同"切断模式；纯洁勇士/page_1365_0480 修源数据 |
| **R6** | M1 range 内 `**区域：**`×57 及其他 154 处 | registry 标签集补"区域"等别名（KN012 机制内解决）；返工后以广义口径（字段值含 `**`）复测归零 |
| **R7** | 文档与基线：交付文档 F1~F6 修正；`verify_spells.py` 白名单清空；N7 表述更新为"三 chunk 补名后保留为独立带标题 chunk"；test_chunk_count 等基线断言**待返工重生成后**统一更新 | 返工收尾时一并提交 |
| **R8** | N6 铜墙铁壁 ST+SR parsed | 接受为良性误报，录入已知问题记录（低优先级，可选修） |

## 5. 对交付文档 §8 五项的裁定

1. **4470 基线**：暂不认可。4470 中混有 8 个 N2 伪 chunk 与 1 个空壳 chunk，且 KN015 的 -3 未发生，总数口径须待 R1~R4 返工重生成后重新核定，届时再更新 `test_chunk_count` / `test_covered_files_match_estimate`。
2. **KN016 剩余处置**：驳回"整体另立 KN019"。核心书 16 个必须本轮修复（R1），仅纯战役设定 9 个可立 KN019 延后。
3. **N6 Guards and Wards**：接受为良性误报（prose 引用含 `**豁免：**` 标签所致，千分之四量级），录入已知问题记录。注意实际 ST **和 SR** 双字段 parsed，交付文档需补正。
4. **ST missing 2008 vs 1598**：基线偏移解释成立（新增 chunk 多来自非标源），但按裁定 1，目标值随返工后基线重校准，本轮不强制。
5. **5 职业补审计**：k3 已直接执行（歌者 162/战斗祭司 225/奥能师 371/猎人 141/调查员 46），恢复确认，**关闭**。

## 6. 复跑入口

```bash
cd pf_data/phase1
python3 audit_k3_kn011_018.py    # k3 独立指标重算（metadata 口径）
python3 vectorizer/verify/verify_spells.py
python3 -m pytest vectorizer/tests/ -q
```

## 7. 审计记录

| 轮次 | 日期 | 审计人 | 结果 | 备注 |
|---|---|---|---|---|
| 1 | 2026-07-30 | k3 | **不验收** | 6 项失实 + R1~R8 返工；KN011/014/015/017/018 核心指标属实达标 |
