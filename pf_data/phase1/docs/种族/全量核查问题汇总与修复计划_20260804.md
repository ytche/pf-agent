# 种族 chunk 全量质量核查 — 问题汇总与修复计划（20260804）

> 交付方：执行模型（deepseek-v4-flash）
> 任务来源：`种族chunk全量质量核查_交接文档_20260804.md`（KN113~KN120）
> 本阶段：六组 subagent 全量排查完成（A~F 六组，覆盖全部 65 个 race_name 与全部聚合文件），问题按机制归并，形成修复计划
> 基线：chunks.jsonl 3184 chunks、pytest 683、verify_races 六检查全过、race cicd 全链

## 一、核查覆盖

| 组 | 范围 | 产出 |
|---|---|---|
| A | 核心种族 9 + 常见种族 11 | 部分符合，属性调整行家族首见 |
| B | 其他种族 + 罕见表 + KN120 个例复核 | 词表缺口实证、KN119 位置修正 |
| C | 罕见种族 + 怪物种族 + KN100 复验 | BoS FCB 丢失、page_814/815 整族吞并、蝮血裔全丢 |
| D | 怪物种族 + 怪物法典 + 内海怪物志 | stat block 伪条目 45/52、霜巨人粘连、HTML 注释泄漏 |
| E | 10 个来源书聚合文件 | **KN115 根因精确定位**、BotC 幽灵 cur、BotS 裸特性丢失 |
| F | KN113 机制实锤 + KN118 定性 | intro 登记覆盖 race_name 实证；神裔血统非缺失而是 mega chunk 未拆分 |

## 二、已否证（勿重复追查）

- 娜迦裔「催眠凝视截断」：正文完整（每天1次/施法者等级=HD/DC11+魅力修正），未复现
- 神裔「人之子 replaces 空」：非解析 bug，源文 206-207 行确无「取代X」字样，归源数据缺口（P3）
- 大地精「罗圈腿」+4：用户确认正确（交接文档已否证）
- 土元素裔「缓慢速度」：误报（交接文档已否证）
- 阴影血脉BoS_影生暗生种族特性 0 chunks：verify_races.py:43 设计排除（KN 登记），非解析缺陷
- KN119 在「内海种族ISR_其他种族替换特性」0018 未复现（文本干净）

## 三、问题按机制归并（21 机制）

### 批次 1 — P1 解析修复（TDD）

| # | 机制 | 症状 | 影响文件 | 根因 | 对应 KN |
|---|---|---|---|---|---|
| M1 | GLUE 误并「章节标题+条目标题」4 星粘连 | 种族特性替换段落整段未切出（敏锐狐妖/特技专家 0 hits）、FCB 0 个切出、title 前缀污染（背景元素×6）、「精灵精灵」文本污染 | BotB 全文件、BotN、ISR 核心、ISR 其他 | `_RE_CN_STAR_CN_GLUE` 把 `**种族特性替换****敏锐狐妖**` 合并；`_RE_QUAD_GLUE_LEADING` 前段限 4 字未覆盖 | KN115 |
| M2 | TITLE 分支 dedup pop 后 cur 未置 None | 幽灵 cur 吞并后续全部内容，10 亚种（妖鬼婆裔~冬鬼婆裔）0 产出 | BotC 替换儿种族选项 | L1100-1107 pop 后缺 `cur=None`（BARE 分支 L1172 有） | 新 KN |
| M3 | 双段标题形态不识别 | `**妖鬼婆裔 替换儿（Annis-Born Changelings） 矿砾之女（Slag May）**` 整行不匹配任何标题正则 | BotC 替换儿种族选项 | `_RE_TITLE` 组1 不容空格、组4 不容中文尾注段 | 新 KN |
| M4 | 属性调整行未独立成 trait | ~20+ 族属性调整粘 intro 尾；page_385 蝮血裔属性行**完全丢失** | 各族详情页 + page_385 | 属性调整行形态（`**+2力量…**` / 裸 `+2力量…`）无独立切出路径 | 新 KN |
| M5 | race_name 被 intro 标题覆盖 | page_11 年龄章节 70 chunks race_name 全为「年龄」 | page_11 | 首个 race_intro 登记 `_race_name` 后全文件继承（F 组实锤） | KN113 |
| M6 | 整族吞并 mega chunk | 伽瑟兰 7trait+7alt+13FCB 全在 1 intro；蒿兰人同款；神裔 6 血统 5102 字符 1 chunk；魔裔 10 血脉 8745 字符 | page_814/815、KN118 文件 | chunk 内嵌套条目（`**条目名（EN）**` / 裸 `条目名：`）无行内拆分路径 | KN118 |
| M7 | FCB 裸文本条目不识别 | BoS 剪影人 15 + 窃影鬼 14 条 FCB 整节静默丢失（条目无加粗标题） | BoS 剪影人/窃影鬼 | 裸 `野蛮人：…`（无 `**`）不匹配 `_RE_BARE_CN_COLON` | 新 KN |
| M8 | PFS 禁用 follow-on 段落粘连 | 矮人 6 处/半身人 5 处点名，先全量清单再修 | 矮人/半身人 | 待全量清单后定性 | KN114 |

### 批次 2 — P2 解析修复（TDD）

| # | 机制 | 症状 | 影响文件 | 根因 |
|---|---|---|---|---|
| M9 | ✅ 已修复 | state 不随种族 H2 重置 | 种族标题切为 alt_trait/race_trait/race_intro（组件误标家族）；背景特性误标 alt_trait | BotB 4、ISR 新种族 2、BotS 3、CtIE、ISR 其他 4 | 章节关键词缺「种族背景特性」；H2 种族标题不重置 state | KN127 闭环 |
| M10 | ✅ 已修复 | 裸中文特性行不识别 | 塞卡利亚 14/洛卡鱼人 9/梭螺鱼人 10 基础特性全并入首条目；双括号 `（Jet）（1 RP）`；水栖精灵 FCB 9 职业并入 0044 | BotS | `_RE_BARE_CN_TITLE` 需 `**` 包裹；裸「中文：」需（EN）；星内冒号形态不匹配 | KN124 闭环 |
| M11 | ✅ 已修复 | `Telepathy，3\nRP` 断行不合并 → 整条并入相邻条目 | ISR 新种族 2 处 | 断行合并正则为字母结尾锚定 |
| M12 | ✅ 已修复 | 「条件」「弱点」「推荐科研发现」「另外」等字段行切伪条目 | 各族 | 词表补「条件」「推荐科研发现」+ 带括号分支「弱点」特例 + page_1015 源数据修复（详见 §六 M12 备忘） |
| M13 | ✅ 已修复 | 取代条款伪条目 | 暮行者/狗头人/怪物种族 | 取代条款行（`X特性取代Y（EN）…`）被当独立条目 + 孤立标点 chunk（详见 §六 M13 备忘） |
| M14 | ✅ 已修复 | 断行/星壳标题合并家族 | 8 处标题拆行失败并入前条目；inline 星后 `：` 不拆（BotB 变换体型粘 0005） | `_split_inline_star_titles` 星后 `：` 守卫 + 断行合并缺口（详见 §六 M14 备忘） |
| M15 | ✅ 已修复 | stat block 字段标题伪条目 | 阵营/生态背景/特殊能力/栖息地与社会 45/52 个伪条目 | AP_怪物 2 文件 | 怪物 stat block 标题形态未被 `_MONSTER_BLOCK_SECTIONS`/词表覆盖（详见 §六 M15 备忘） |
| M16 | ✅ 已修复 | 霜巨人整章粘连 | 整章源数据单行连排 → 大量条目吞并 | 霜巨人页 | 源数据单行（>3000 字符行长）连排，`_split_long_lines` 边界不覆盖（详见 §六 M16 备忘） |
| M17 | ✅ 已修复 | 表格残留 | 兽态人 9 血统表并入起始语言；内海怪物志 4 文件标题拆行+HTML 注释泄漏 | ISR 新种族、内海怪物志 4 文件 | 表格行未按条目切出；HTML 注释未剥 | 新 KN（M17c H2 吞并守卫，详见 M17 备忘节） |

### 批次 3 — 源数据/清洗/范围

| # | 机制 | 症状 | 处理 |
|---|---|---|---|
| M18 | ✅ 已修复 | 源数据删除 69 处（32 文件）+ 14 处保留项（合法规则注释） | 见「批次 3 进度」+「M18 修复要点备忘」 |
| M19 | ✅ 已修复 | 灵猴族 page_10 汇总「攀爬30尺」vs page_379「20尺」vs 官方 20 尺 | 源数据修正（KN117） |
| M20 | ✅ 已修复 | page_812 阿斯托莫伊人 0 chunks；神裔血统（已定性未拆分归 M6）；BoS 影生暗生设计排除 | 走 KN118 流程（audit_chm_scope_gap.py） |
| M21 | ✅ 已修复 | KN120 剩余个例清单（半身人 4/人鱼/大地精/娜迦裔/树蛙人/矮人） | 随批次 1/2 顺带复核 + M21 收尾（详见 M21 备忘节） |
| M22 | ✅ 已修复 | KN116 审计残留：ISR×2 + BotC 替换特性文件 107 条系统性误标 race_trait（M9 声明与现状不符） | 审计复查发现 → H2 章节判定 + alt_trait 三排列关键字 + pending_race_h2 信号优先（详见 M22 备忘节） |

**批次 3 全部闭环（2026-08-04）**：M18（KN119）→ M19（KN117）→ M20（KN099 局部）→ M21（KN120 部分闭环）→ **M22（KN116 审计残留修复）**

## 四、修复计划

- **执行顺序**：批次 1（M1→M2+M3→M4→M5→M7→M8→M6）→ 批次 2（M9→M10→M11→M12→M13→M14→M15→M16→M17）→ 批次 3（M18→M19→M20→M21）
- **TDD**：每个机制先写失败测试（TestCluster 先例），红→绿；M8 先全量清单（PFS 禁用 follow-on 模式 grep）再定修复
- **每机制回归链**：pytest 全量 → pipeline 重跑 → verify_races 六检查 → 判别器回归静默丢失 0 → 不变量闸口 → chunk 数变化全量定性 → KN 状态更新 → git 提交（含文档同步）
- **公共层改动**（normalize_common 等）跨类目回归：race + feat 两侧测试（KN112 教训）
- **新 KN 登记**：M2~M4、M6（KN118 已登记）、M7、M10、M14~M17 修复后按机制更新登记；未修项如实登记不顺手修

## 五、批次 1 首项（M1）修复设计要点

1. `_RE_QUAD_GLUE_LEADING` 前段 `{2,4}` → `{2,10}`，第二段为「中文（EN）」标题形态时补星拆出整行：
   `**种族特性替换****敏锐狐妖（Keen Kitsune）**：正文` → `**种族特性替换**\n**敏锐狐妖（Keen Kitsune）**：正文`
2. 新增行首裸 4 星拆行（ISR 核心 `精灵****精灵角色…`）→ `**精灵**\n精灵角色…`
3. `_RE_CN_STAR_CN_GLUE` 守卫：组1 前字符非 `*`（裸文本 4 星）不合并（防漏网）
4. `_DEDUP_SECTION_WORDS` 补「背景元素」（BotN 章节词，拆行后空壳 cur 防残留）

## 六、批次 1 进度（2026-08-04）

| 机制 | 状态 | 结果 |
|---|---|---|
| M1 | ✅ 已修复 | BotB 12→71、BotN 13（字段链并入）、pytest 692、六检查全过；行中 4 星章节粘连二次修复（去锚定 + 章节词感知守卫）；伴生 BotN 亚种字段链词表（`_ARG_FIELD_LABELS` +10 词）；KN115 闭环、KN121 登记（M18 前置遗留） |
| M2 | ✅ 已修复 | TITLE 分支 pop 后补 `cur=None`；BotC 10 亚种全部独立切出（8→20） |
| M3 | ✅ 已修复 | 双段标题拆行（`_RE_DUAL_CN_TITLE`）；含伴生词表回归（鬼婆血统觉醒真条目被 M1 字段词表误吞，L1127 收窄 `_ENTRY_FLOW_FIELD_LABELS`） |
| M4 | ✅ 已修复 | 属性行独立切出（`_RE_ABILITY_ADJ_TITLE` 分支，race_trait 态产「属性调整」条目）；31 文件 +31、蝮血裔丢失修复、pytest 698 |
| M5 | ✅ 已修复 | race_overview 行 race_name 用行 title（processors 层特判，不继承文件级 intro 登记）；page_11 68 行「年龄」→ 各族名（34 族 × 2 行）、全库余 2 个年龄章节正确值；pytest 701、六检查全过、3296 结构零变化；KN113 闭环 |
| M6 | ✅ 已修复 | 血脉亚种表格行拆分（`_fix_heritage_table_rows`）：`|  | 血脉名（中文）描述…祖先：…替换属性调整：… |` HTML 表格单元格跨行 → 拆出 `**血脉名（中文）**` + 字段链内容行，缩进描述段并入血脉条目；神裔 6 血脉 + 魔裔 10 血脉独立切出（mega 5102/8745 → 743~912 字符条目）、分隔行剥除；+16、0 移除 0 误切、pytest 721、六检查全过、闸口全过（>5000 字符 3→1）；KN118 闭环（非输入缺口，F 组定性实证）；能力变体随机表（d% 双列）保持整表登记 M17 |
| M7 | ✅ 已修复 | 裸职业行三形态全切出（裸版 + 星壳开 + 星壳闭）：剪影人 15 + 窃影鬼 14 + 伽瑟兰 12 + 蒿兰人 10 + 海洋之血 6 + 沼蜍人 4 = 6 文件 +64、0 移除 0 误切、pytest 710、六检查全过、闸口全过；KN126 闭环 |
| M8 | ✅ 已修复 | PFS 禁用/修订尾注拆行（`_fix_pfs_tail_notes`）：62 处 8 文件 5 形态中 4 形态修复——形态 A `**X（EN）** PFS禁用`（`_RE_PFS_TAIL_NOTE`）、形态 B 钢铁公民 `（PFS修订：…` 跨行长尾注（`_split_title_glue_paren` 拆行后 `_RE_WS_TITLE` 跨行粘回 → 去行首括号防粘）、形态 C 同行粘连 `PFS禁用**出自《…》**`、空格形态 `**X（EN） **`（星内尾空格，拆行时清掉使 `_RE_TITLE` 可匹配）；矮人 6 + 半身人 6 + 精灵 9 + 人类 9 + 半精灵 15 + 侏儒 6 + 半兽人 5 = 8 文件 +55、0 移除 0 误切、pytest 717、六检查全过、闸口全过；KN114 闭环；形态 D（page_934 幽暗地域半兽人标题星壳闭合在次行）与 page_398 星内 `（PFS不可用）` 字段行（非标题行）登记不修 |

## 七、批次 2 进度（2026-08-04）

| 机制 | 状态 | 结果 |
|---|---|---|
| M9 | ✅ 已修复 | H2 state 重置（`-source:` 注释信号 + `_race_h2` 防弹壳并入 + 散文升级四守卫）；半人马「陷阵士」标题扩写、H2 页眉/注释剥离 9 处；pytest 734、3431 零丢失（详见 M9 备忘节）；KN127 闭环 |
| M10 | ✅ 已修复 | 中段分裂合并 + BotS 裸特性 + 图片引用剥除；pytest 756、chunk 3572、**空 text 13→0**；KN124 闭环（详见 M10 备忘节） |
| M11 | ✅ 已修复 | `Telepathy，3\nRP` 断行合并（`_RE_CROSS_CN_DIGIT_EN`）；pytest 758、chunk 3573（详见 M11 备忘节） |
| M12 | ✅ 已修复 | 「条件」「弱点」「推荐科研发现」等字段词表 + page_1015 源数据修复（详见 M12 备忘节） |
| M13 | ✅ 已修复 | 取代条款伪条目（词表 + 判据，详见 M13 备忘节） |
| M14 | ✅ 已修复 | 同行多条目拆行（三轮迭代，详见 M14 备忘节） |
| M15 | ✅ 已修复 | stat block 章节标题伪条目 45/52 消除（详见 M15 备忘节） |
| M16 | ✅ 已修复 | 霜巨人整章粘连（`_wrap_bare_cn_en_lines_race` 去锚定 + 源数据拆行 3 处）；pytest 772、chunk 3591、diff +43 全定性零丢失（详见 M16 备忘节） |
| M17 | ✅ 已修复 | 表格残留家族收口：M17a 星壳错位 50 处（内海怪物志 4 文件，源数据修复）+ M17b 兽态人 9 血统镜像表格 + 裸 `---` 剥除（KN123）+ M17c H2 吞并前缀守卫；pytest 784、chunk 3612、verify 六检查全过、diff 52 丢失全定性零信息丢失（详见 M17 备忘节） |

### M1 修复要点备忘（回归链已走通）

- **行中 4 星章节粘连**（BotN 源 L13）：`_RE_QUAD_GLUE_SECTION_TITLE` 必须无 `^` 锚定 + 替换串 `\n` 前缀——行首锚定版对 `…那里。**背景元素****语言天才…**：` 不命中，GLUE 守卫又拦住合并 → 整行吞并（BotN 18→8 chunks 回归实证）
- **GLUE 守卫升级**：仅「前字符为 `*`」不够——`**背景元素****语言天才` 前字符也是 `*`。须章节词感知（`_GLUE_GUARD_WORDS` = `_SECTION_KEYWORDS` ∪ `_DEDUP_SECTION_WORDS`，前段窗口 14 字符）——章节词命中不合并（拆行处理），星内拆裂（`**魅****影身姿`）合并
- **字段链测试假阳性教训**：intro 态字段行并入 cur 是既有行为——BotN 亚种字段伪条目的测试必须带章节行（`**种族特性**`）模拟 race_trait 态，否则测试假绿（真实文件 race_trait 态才切伪条目）
- **BotN 亚种字段**（`**祖先**：` 星壳形态）在 `_ARG_FIELD_LABELS` 补词后并入亚种条目（20 伪条目消除）；「属性调整」入词表与 M4（`**+2力量…**：` 值星壳形态独立）不冲突——形态不同

### M2/M3 修复要点备忘（回归链已走通，KN122 闭环）

- **M3 双段标题拆行**：`_RE_DUAL_CN_TITLE`（`^\*\*([一-鿿]{2,8}) ([一-鿿]{2,4})（([^）]+)） ([一-鿿]{2,6})（([^）]+)）\*\*$`）须 `re.MULTILINE` 锚定每行行首（文件中部标题，单行文本测试会假绿）；拆行输出 `**X1X2（EN1）**` + 变体行；**变体行必须星壳包裹** `**（X3 EN2）**`——公共层 `_RE_WS_TITLE`（normalize_common.py L663）的 `\s+` 跨行匹配会把裸 `（矿砾之女 Slag May）` 行拼回标题并打 `[[fc:whitespace]]` 标记（首版实证）；星壳阻断该模式，split prose 分支剥壳后正文干净；条目名取亚种名（官方名 Annis-Born Changeling），变体名（Slag May 俗称）进 text 保检索
- **词表回归教训**：M1 补「鬼婆血统觉醒」进 `_ARG_FIELD_LABELS`（BotN 字段链用途）误伤 BotC 真条目标题（带 EN 括号形态被 split L1127 拦截并入前条目）——字段词补表必须核查「带括号标题形态」场景；修复：L1127 拦截词表 `_ALL_FIELD_LABELS` → `_ENTRY_FLOW_FIELD_LABELS`（ARG 字段词带括号形态全库无字段行场景，BotN 星壳冒号形态走 COLON 分支不受影响）
- **M2 pop 后 cur=None**：TITLE 分支 dedup pop 后置空（与章节分支 L1098-1102、裸标题分支 L1228-1232 对称）——pop 后同名标题合并 continue、散文并入已弹死 dict 整段丢失；修复后行为「章节条目保留散文」
- **+12 全量定性方法**：git stash 旧代码 → pipeline 重跑 `/tmp/race_old_3253` → diff 确认全归因 BotC 单文件（8→20 = 10 亚种 + 鬼婆血统觉醒 + 替换儿亚种 intro），其余文件零 diff

### M5 修复要点备忘（回归链已走通，KN113 闭环）

- **根因**：`build_chunk` 首个 race_intro 登记 `_race_name = template.title`（page_11 文档主标题「年龄」），`infer_metadata` 兜底链（intro 登记 > 路径归属 > 行 title）使全文件 68 个 overview 行 race_name 继承「年龄」
- **修复**：`infer_metadata` 3.1 核心字段对 `ct == "race_overview"` 特判 `md["race_name"] = chunk.title`（汇总页表格行 title 即种族名）；非 overview 保持兜底链
- **影响面**：page_11 68 行（34 族 × 2 行：年龄表+身高体重表）；page_10 无 intro 现状已=title 不变；page_12（intro=「矮人」与行名一致）特判前后等价；全库仅余 2 个「年龄」（race_intro「年龄」+ race_trait「身高和体重」，属年龄章节本身，正确）
- **测试**：processor 层 3 个（overview 用行 title / 非 overview 保持继承 / overview 行 race_name_en 不联动），helper 需手动设 `chunk.component_type = "race_overview"`（`_build_chunk_template` 默认 = category "race"）

### M7 修复要点备忘（回归链已走通，KN126 闭环）

- **三形态归并**：FCB 裸职业行共 3 种形态——①无星壳 `野蛮人：正文`（剪影人 15/窃影鬼 14）②星壳闭 `**吟游诗人：**正文`（冒号在闭合星内，page_814 伽瑟兰/815 蒿兰人 + 海洋之血/沼蜍人）③星壳开 `**职业**：正文`（既有 `_RE_BARE_CN_COLON`）
- **新增两个正则**：`_RE_BARE_CN_COLON_UNSTARRED`（组1 汉字，含 `/`）、`_RE_BARE_CN_COLON_STAR_CLOSED`；都只配 `state == "fcb_entry"` 态守卫——其他态（intro/散文/字段）并入 cur 保持既有行为，态守卫隔离零误伤
- **三守卫**（两分支共用）：①组1 含 fcb 章节关键字（`_RE_FCB_SECTION_WORD` = `天赋职业|职业奖励`，BotB `天赋职业选项吟游诗人：` 粘连行）不切；②cur 空 text（page_13 标题独立成行后的武器列表正文行）并入；③组2 以「边栏」开头（page_1456 引述行）不切
- **剥壳**：裸版分支正文以 `**` 开头剥 2 字符；星壳闭分支 `m.group(2)` 天然剥壳
- **全量定性 +64 构成**：61 新 key（剪影人 15 + 窃影鬼 14 + page_814 12 + page_815 10 + 海洋之血 6 + 沼蜍人 4）+ 3 既有 key 增行（海洋之血骑士/德鲁伊/游侠——洛卡鱼人裸行 + 水栖精灵星壳闭两章节合并），0 移除 0 误切；text 变更 7 个真实收缩（FCB 摘出）+ 3 个重复 key 对比偏差（diff 按 key 去重，海洋之血实为增行非内容变化）
- **说明行粘尾登记**：剪影人「阴影讲述人」/沼蜍人「游侠」text 尾部粘 FCB 章节说明行（「以下选项对所有…可用」）——既有行为，归 M9 家族批次 2 处理
- **测试**：簇 O（6 个）+ 簇 O2 TestClusterO_FcbStarClosed（3 个，page_814 真实混合形态：裸行正文带星壳 + 星壳闭行）；stash 回退验证红→绿；pytest 707→710

### M8 修复要点备忘（回归链已走通，KN114 闭环）

- **根因**：`_RE_TITLE` 行尾 `$` 锚定不容尾注——`**X（EN）** PFS禁用` 失配 → 整行并入前一条目（矮人 6 处：远古敌意粘精类思维/勤劳城市人粘钢铁公民等）
- **`_fix_pfs_tail_notes` 插入点**：`_wrap_bare_cn_en_allcaps` 之后、`_normalize_whitespace_title` 之前——钢铁公民 `（PFS修订：` 长尾注若不先拆，`_RE_WS_TITLE`（`\s+` 跨行）会把拆出的尾注行粘回标题并打 `[[fc:whitespace]]` 标记
- **⚠️ 正则必须 `re.MULTILINE`**：normalize 处理整段文本，`^`/`$` 须锚定每行行首/行尾——单行文本测试会假绿（首版实现无 MULTILINE，单行直调成功、整段 normalize 失败）
- **形态 B 链路**：`_split_title_glue_paren` 先拆 `**X**（PFS修订：…` 成两行 → `_RE_WS_TITLE` 的 `\s+` 跨行又粘回 → 修复：去行首 `（`（`PFS修订：` 前缀不再被 `_RE_WS_TITLE` 匹配），信息无损
- **星内尾空格形态**（page_934 萨满加持/精类思维 `**X（EN） **`）：HTML 转换残留 `） **`，拆行后 `_RE_TITLE` 仍失配（`）` 后须紧跟星壳闭合）→ 拆行时 `re.sub(r"[ \t]+\*\*$", "**", 组1)` 清星内尾空格
- **登记不修**：形态 D（page_934 幽暗地域半兽人——标题 3 行断行 + 星壳闭合在次行，`**…（Darklands Half-Orcs \n(Gloomkin)）  \n** PFS禁用`，规则不覆盖）；page_398 `来源：…（PFS不可用）**` 是字段行尾注非标题问题
- **全量定性 +55 构成**：半精灵 15 + 精灵 9 + 人类 8 + 矮人 6 + 侏儒 page_1366 6 + 半身人 6 + 半兽人 page_934 5，0 移除 0 误切；PFS 标记保留在条目 text 首行（检索端可查 PFS 状态）

### M6 修复要点备忘（回归链已走通，KN118 闭环）

- **根因**：神裔/魔裔亚种 = HTML 表格（`|  | 血脉名（中文）描述… |`），**表格单元格内跨行**（L93→L102 缩进续行），一行含整条血脉全部字段链（祖先/典型阵营/替换属性调整/替换技能加值/替换类法术能力）——无行内拆分路径，6/10 血脉整段吞进 1 个 mega chunk
- **`_RE_HERITAGE_TABLE_ROW` 必须 `re.DOTALL`**：单元格跨行（单行正则失配，首版 3415 不变实证）；`.*?` 非贪婪停在第一个行尾 ` |`（中间缩进续行行首空格无 `|`，安全）
- **拆分输出**：`**血脉名（中文）**` 标准标题 + 字段链内容行（内容行首无星，散文并入刚建条目；字段标签「祖先/典型阵营/替换属性调整/替换技能加值/替换类法术能力」不在 `_RACE_FIELD_ALT` 不拆行）
- **分隔行剥除**（`_RE_TABLE_SEP_ROW`）：`| --- | --- |` 转空行——不剥则每血脉 text 尾粘分隔行（M17 表格残留/KN123 `---` 残留同源，本次顺带全库处理，零信息损失）
- **KN118 定性反转**：神裔血统变体**非输入缺口**——内容在 mega chunk 内（F 组定性正确），拆分后 6 血统独立恢复
- **登记 M17**：能力变体随机表（`| d% | 能力 |` 双列，神裔 4352/魔裔 4183 字符）保持整表——d% 随机引用表整表命中是合理检索行为，M17 再评估
- **结果**：神裔亚种 251（章节标题+说明）+ 6 血脉（743~816）；魔裔亚种 299 + 10 血脉（768~912）；全 race_trait ✅

### M9 修复要点备忘（回归链已走通，KN127 闭环）

- **根因**：章节标题行命中 `_RE_TITLE` 分支建条目**不更新 state**（state 只在 `_match_section`/pending_race_h2/散文升级处变）——BotB 4 / ISR 新种族 2 / BotS 3 / CtIE / ISR 其他 4 族组件误标家族、背景特性误标 alt_trait
- **H2 识别扩展**：`_RE_H2_BARE_CN`（`#{2,3}` + 空格前缀 + 转星取末段）+ `_RE_H2_STAR_HASH`（`**## X**` 剥壳）+ `_RE_SOURCE_COMMENT`（`-source:` 注释信号）+ `_split_bare_section_titles` 井号/注释行不拆
- **pending_race_h2 信号**：H2 标题行先行 → 空 text 首行并入（`_race_h2`）+ 同名合并补 name_en + 标题重置——半人马「陷阵士」→「半人马陷阵士」（内海怪物志 H1 页眉 + `-source:` 注释剥离，标题扩写合理）
- **散文升级分支**（BotB 型 `**种族名**　　散文` 拆行形态）：升级 race_intro + state 重置——**4 守卫缺一不可**（本次会话 3 轮回归实证）：
  1. 空行不升级（S1）
  2. 标题形态 `^[一-鿿]{1,8}[（(]` 不升级——page_26 回归
  3. **数字值行 `^[+−-]?\d` 不升级**——page_16 半身人 stat block「敏捷」+ 值行触发升级 → 4 字段（小型体型/缓慢速度/武器熟悉/起始语言）被 `_RE_BARE_CN_COLON` 吞并，簇 S3 测试（数字+标题形态判据）
  4. **含中文字符 `[一-鿿]` + state 白名单（race_trait/alt_trait/race_archetype/fcb_entry）**——page_163 沙包（`】` 无汉字行）/护颈（race_item 态）误升 race_intro 吞「弱点」，簇 S4 测试
- **判据组合语义**：条目流态（race_item/feat/spell）裸标题+散文是正常条目流不升级；残留态（race_trait 等 4 态）裸标题+散文才可能升级
- **结果**：3431 = bak 3431（零丢失零新增），全库 diff 仅 text 级改进（H2 页眉/注释剥离 9 处 + 标题扩写补全 3 处）；pytest 730→734、verify_races 六检查全过、判别器静默丢失 0、闸口全过（空 text 4 既有 KN124、>5000 1、book '?' 2390、toc 0）

### M10 修复要点备忘（回归链已走通，空 text 闸口 13 → 0 闭环，KN124 闭环）

- **根因**：三层——①HTML 加粗段拆裂中段分裂（`**高等地精****自燃术（****Greater Fire Body, Ex****）**`）被 glue 拆行成伪条目（地精自燃术/精通/高等/地精铳士/灌魔恶臭墨水 5 条目丢正文）；②BotS 裸特性（`**替换自然魔法**` 空壳，正文在甜气条目「这个种族特性取代了自然魔法」）+ 章节壳（`**种族背景(Race Traits)**(来源…`、`**种族特性替换**`）；③Obsidian 图片引用残留 5 处（`![[图片]](URL)`，`_strip_http_links` 剥 URL 留 `![[图片]` 残壳）
- **中段分裂合并**：`_RE_QUAD_NESTED_TITLE` 组2 扩展收 HTML 加粗拆裂中段（`**高等地精****自燃术（****EN****）**` → 单标题）+ 组5 类型标签 `〔战斗〕` 保留在星壳内（`_RE_TITLE` 组3 星壳内才识别）；函数内先调 `_merge_cross_line_en`（幂等，处理 `（****Fire \nBody, Ex****）` 跨行 EN）；**调用点移到 `_split_quad_glue_titles` 前**（glue 组2 以中文开头会先拆行成伪条目；章节+条目粘连 `**章节****条目（EN）**` 括号无星天然豁免）
- **弹壳判据**：`_is_dedup_section_cur` 加 `kind == "alt_trait"` 弹壳（无正文替代特性壳——正文在子条目 text 检索等价命中，K6 破敌之锤同款；有正文的替换墨汁云不受影响）+ `_DEDUP_SECTION_WORDS` 加「种族背景」（page_940 卡萨达人章节壳，正文跨行 `(来源 \nPathfinder…)**四手灵巧**` 独立切出）
- **字段词表**：「起始属性」同时入 `_ENTRY_FLOW_FIELD_LABELS` + `_ARG_FIELD_LABELS`（page_1412 鼠族 `**骑乘用巨鼠动物伙伴****起始属性: \n体型** 中型;` 粘连拆行后裸冒号行在 race_item/race_trait 两态都被切伪条目——两层都加，测试簇须用真实章节行触发 race_item 态）
- **图片剥除**：新增 `_RE_IMAGE_REF = r"!\[\[图片\]\]?(?:\([^)]*\))?"`，插在 `_merge_table_continuation` 后 `_strip_http_links` **前**（URL 先剥会留 `![[图片]` 残壳）；剥除后 text 空 → 触发弹壳逻辑；race 无 pfs_eligible 不需公共 `_mark_pfs` 的 `[[PFS]]` 标记
- **结果**：pytest 752→**756**（簇 U 中段分裂 5 + 簇 V M10 三形态 4，零回归）、chunk 3568→**3572**、verify_races 六检查全过、判别器静默丢失 0、闸口全过（**空 text 13→0**、>5000 1、book '?' 2447/上限 2450、toc 0）；diff 定性：5 合并产物 text 完整（如高等地精自燃术含「14级时，炸弹人将‘元素身躯IV’…」）、伪条目全消除、图片残留 5→0（最后 1 个含「图片」为译者吐槽 `【…google图片一下。】` 属 M18 范畴，留批次 3）

### M11 修复要点备忘（回归链已走通）

- **根因**：公共 `_RE_CROSS_EN` 组1 `[a-zA-Z,]` 只收字母/逗号——`Telepathy，3 \nRP）` / `Characters，11 \nRP）`（ISR 新种族 2 处，能力值/引文数字行）行尾数字失配 → 标题不成形，「有限心灵感应」整段粘「知识渊博」尾部（text 尾 `**` 碎片）、「勒珊塔灵族角色」粘 intro
- **修复**：race 层 `_merge_ability_adj_lines` 加第三正则 `_RE_CROSS_CN_DIGIT_EN`（M10 同函数扩展）：`([A-Za-z][，,]\d+)[ \t]*\*{0,4}[ \t]*\r?\n[ \t]*([A-Za-z]{1,3}\*{0,4})` → `\1 \2`。守卫：组1 字母紧贴逗号（千分位 `1,500\nGP` 逗号前数字不触发）；组2 短缩写 ≤3 字母（正文「，数字 \n长词」不误并）；`\d+`（首版 `\d` 单数字被 `Characters，11` 第二位数字挡住换行失配，TDD 捕获）
- **定性**：有限心灵感应独立切出（+1，后续 14 key chunk_id 顺延内容不变）；「勒珊塔灵族角色」标题断行碎片→完整形态仍在 intro（M9 `_race_h2` 防弹壳吞并的既有行为，角色定位属 intro 语义，检索等价命中，不切独立条目）；0013 知识渊博尾部 `**` 残留顺带清除；源数据确认无缺 `）`（整理文件 `RP）**：` 完整，此前 sed/cat 显示缺失为 UTF-8 终端误读）
- **结果**：pytest 756→**758**（簇 W 2 个，TDD 红→绿：断行合并进 name_en `Limited Telepathy 3 RP` + 小节标题切出）、chunk 3572→**3573**、verify_races 六检查全过、判别器静默丢失 0、闸口全过（空 text 0、>5000 1、book '?' 2447、toc 0）

### M12 修复要点备忘（回归链已走通）

- **根因**：字段词表缺口三处——①「条件」（page_440/441 魔法物品制造字段链）与「推荐科研发现」（page_166 地精炼金术士）裸标题冒号行在 race_item/race_feat 态被 L1662 词表判定切伪条目（KN100 家族）；②page_204 狗头人 stat block `**弱点（Weakness）**`（星壳嵌套剥除后带 EN 括号形态，全库仅此一处）走 `_RE_TITLE` 带括号分支（L1544 只查 `_ENTRY_FLOW_FIELD_LABELS` 不查 ARG——设计决策防「鬼婆血统觉醒（Awakened Hag Heritage）」真条目标题误吞）→ 切「弱点」伪条目；③page_1015 编注段落 `**另外****玩家角色与CR…**` 粘连星壳（HTML 加粗拆裂产物）→ 「另外」切伪条目（monster_block）
- **首版两处词表补充依据错误 → 实施中收窄为终版**：
  - ENTRY_FLOW 首版补「条件」「弱点」「推荐科研发现」——「弱点」**移除**：page_163 唯一裸 `**弱点**：` 行是速查残留段正文（【速查：吸血鬼弱点，译者**沙包**】吐槽的续行，M18 吐槽删除范畴），误加后弱点正文被并入沙包伪条目（S4 簇回归捕获）；stat block 场景是带括号形态不经此词表
  - ARG 首版补「弱点」——**移除**：page_204 真实形态走带括号分支不查 ARG（从未生效，簇 X 首版通过全靠 ENTRY_FLOW「弱点」）；且裸形态经 `_ALL_FIELD_LABELS` 误伤 page_163
  - **终版**：带括号分支 L1544 加 `or name == "弱点"`（page_204 stat block 字段并入狡诈，全库唯一命中）；裸形态不入任何词表（page_163 速查段保持独立条目，S4 簇锁定行为）
- **簇 X 测试修正**（TDD 完整性规则允许——断言业务依据与真实源数据不符）：`test_weakness_item_merged` → `test_weakness_item_kept`（docstring 依据「page_163 stat block」不存在——page_163 无 stat block 弱点字段行，唯一裸弱点是速查段；断言改为保持独立与 S4 一致）；`test_weakness_trait_merged` 去 `"弱点" not in by_name`（共享输入与场景 2 裸弱点独立条目互斥）
- **page_1015 源数据修复**：`**另外****玩家角色与CR…**` → `另外，玩家角色与CR…`（剥 `**另外**` 粘连星壳补逗号 + 句尾剥 `**`，字节级保留 `\r` 行尾）
- **结果**：pytest 758→**762**（簇 X 4 个 TDD 红→绿，终版修正后簇 X + S4 共 6 passed 零回归）、chunk 3573→**3567**（-6 全定性：'另外' 真消失 1（编注并入）+ 词表并入 5——page_166 -1 / page_204 -1 / page_440 -2 / page_441 -1，其余「消失」chunk_id 序号偏移 text 完全相同）、verify_races 六检查全过、判别器静默丢失 0、闸口全过（空 text 0、>5000 1、book '?' 2441、toc 0）
- **diff 定性**：并入实例——弱点并入 1（page_204 狡诈「强光敏感」）、条件并入 8（page_440/441 奇物字段链：强息药水/静肠水/尾钩/石肤腰带/石缚权杖等）、科研发现并入 1（page_166 地精炼金术士）；零静默丢失
- **过程教训**：stash 法 diff 定性与并行会话（audit-118）的 stash 操作互相干扰——stash push 后并行会话创建新 stash 置顶、pop 弹到并行 stash 污染工作区（专长文档 2 文件混入）；本窗口靠 pop 输出 hash（02bdc6ff）从 object 库直接 `git checkout <stash> -- <file>` 找回 M12 终版。此后 stash 对比须：命名 stash + pop 后立即 grep 验证 + 留意并行会话活跃度

### M13 修复要点备忘（取代条款伪条目，回归链已走通）

- **形态与分布**：替换特性条目后的独立行 replaces 说明，全库 16 处——带 EN 括号 4 处被误包（PA 暮行者 2 处跨行 `此特性取代腐化抗性（ward against \ncorruption）.`、格拉里昂狗头人 1 处同行 `该特性取代防御（Armor）。`、怪物种族 page_940 1 处跨行无句点 `此能力取代偏好地形（favored \nterrain）`）+ 纯中文 12 处（`此特性取代技能奖励。` 等，不匹配 EN 括号正则，既有行为并入正常，回归保护）
- **根因（双转星点）**：
  1. 公共层 `_wrap_bare_paren_lines`（A 分支）守卫 5 只拦「中文句号 `。` + >30 字长散文」——`此特性取代腐化抗性（ward against corruption）.` 是**英文句点**短行，穿透全部守卫被包成 `[[fc:elided]]**…**\n.`（elided 标题行 + 句点拆出 text='.' 伪条目，信息丢失 + 伪条目双重）
  2. race 特有 `_wrap_bare_paren_colon_titles`（`中文（EN）` 无冒号分支）：page_940 跨行 EN 被 `_fix_quad_nested_title` 合并后成无句点形态 `此能力取代偏好地形（favored terrain）` → 转星成 `**…**` 普通星标题切伪条目
- **修复**：公共层 `_wrap_bare_paren_lines` 守卫链新增 R10 扩展 `_RE_REPLACE_CLAUSE`（`^(?:此|该|这(?:一|些)?)(?:种族|职业)?(?:特性|能力|动作)取代`，normalize_common 模块级，注册进 race.py import）+ `_wrap_bare_paren_colon_titles._repl` 同守卫；删 `_wrap_bare_cn_en_lines_race._repl` 内 M12 误加的未生效死守卫（该函数正则要求「中文 空格 英文」，取代条款是括号形态从不匹配——**教训：守卫必须验证触达**，单函数测试不产生 ≠ 链中不产生，须逐步模拟完整 normalize 链定位真实转星点）
- **TDD**：簇 Y（2 测试）红→绿——`test_replace_clause_not_item`（3 种形态不得切伪条目 + 无孤立标点 chunk）+ `test_replace_clause_merged`（取代条款并入劫运代理者/阎摩执行官/龙之喉/碎甲箭/弃子 text）
- **结果**：pytest 762→**764**（簇 Y 2 个，feat 侧公共层回归零影响——feat 源无独立行取代条款，全量 764 全绿）、chunk 3567→**3564**（-3 全定性：3 个伪条目消失（`此特性取代腐化抗性`=`.`×2 + `该特性取代防御`=`。`×1）+ 4 个条目 text 恢复取代条款（劫运代理者/阎摩执行官/龙之喉/碎甲箭），`id 新增: []` 零静默丢失）、verify_races 六检查全过、判别器静默丢失 0、cicd 全链（pytest + pipeline + 验收 + 闸口）全过（空 text 0、>5000 1、book '?' 2441、toc 0）
- **diff 定性**：worktree 法（`git worktree add /tmp/pf_agent_m13_baseline HEAD` → 基线 pipeline → 独立输出目录对比）——避开 stash 法（并行会话 audit-118 未提交改动 2 文件，stash push 会连带暂存污染，M12 教训重演风险）；内容级对比确认净 -3 全归因

### M14 修复要点备忘（同行多条目拆行，三轮迭代闭环）

- **形态**：HTML 段落合并致两条目同行粘连——行首星标题（星后冒号守卫跳过）+ 行中第二个星标题连带不拆（BotB 变换体型粘 0005：`**种族特性替换****特技专家（Acrobatic）**：正文…**变换体型（Change Size，Su）**：每500只…`）；另有断行合并缺口（page_362 等 8 处标题拆行失败并入前条目，`Telepathy，3\nRP` 等）
- **三轮迭代（3564 → 3573 → 3553 → 3571）**：
  1. v1 行首分支加递归 `_split_line(after)` + search 分支拆行 → +9 但含 2 伪条目 + 4 处毒液块字段行退化（`**类型**`/`**强韧豁免（DC）**` 等字段标签被递归拆出 + 星壳丢失）
  2. v2 词表守卫 `return line` 整行跳过 → -11 丢失：吸血裔 5 + 翼龙人专长 5 + 翼龙人特性 4（行内第一个星壳是 `**先决条件**` 字段标签，行尾真标题连带不拆——**词表守卫不能整行返回**）
  3. v3 最终守卫：search 分支 while 循环——字段标签命中（星壳头部中文 ∈ `_ALL_FIELD_LABELS` = `_ENTRY_FLOW_FIELD_LABELS | _ARG_FIELD_LABELS`）**跳过继续扫描**（`search(line, m.end())` 后 continue，不整行返回）；其余守卫 `return line`：`_RE_FIELD_LABEL_LEAD`（after 以「标签：」开头，字段链形态 `**标签：**值`）、before 以 `】` 结尾、after 以 `**`/`（`/`(`/空格/`\n`/`－`/`-` 开头（字段值形态）→ **3571 = 基线 + 7 纯新增全定性**
- **终态 diff 定性**（worktree 法，`/tmp/pf_agent_m14_baseline`）：纯删除 0（零静默丢失）、纯新增 7（最终拥抱/命理馈赠/诺斯弗拉图裔/多重语言/熟练技艺/上级变身者/变换体型——全为行内/行尾真标题拆出）、成对 12 对（生于光明/语言天才/大忽悠/狗头人陷阱/一大袋酸液倒在头顶上/骑乘用巨鼠动物伙伴/毒性/树蛙毒/黑沼蛛毒/蝮血毒/敏锐狐妖/特技专家——text 尾部变化：拆出标题的条目 text 变干净 + 毒液块字段行粘连恢复）
- **TDD**：簇 Z2（2 测试）——`test_field_chain_not_split`（page_26 娜迦裔毒刺 4 星粘连形态 `**毒刺（****Poisonous \nSting, Ex****）**` 完整链模拟：names==["毒刺"] 且 text 含 `**强韧豁免（DC）**` 星壳不丢）+ `test_hyphen_field_not_split`（page_312 树蛙人昏睡毒素 `**初始效果**－…` 不拆伪条目）；簇 Z3（1 测试）——`test_field_label_not_blocking_tail_title`（page_716 翼龙人专长 L2 `…**先决条件**：…**龙人秩序（Draconian Law）**` 字段标签后行尾标题仍拆出）
- **结果**：全量 pytest 765→**768**（簇 Z2 2 + 簇 Z3 1，race 侧 cicd 收集 361 全绿）、chunk 3564→**3571**（+7 全定性）、verify_races 六检查全过（alt_trait 635、replaces 90.6%）、判别器静默丢失 0、cicd 全链（pytest + pipeline + 验收 + 闸口）全过（空 text 0、>5000 1、book '?' 2441、toc 0）
- **过程教训**：①递归拆行须防字段标签（词表判断，`_ALL_FIELD_LABELS`）；②词表守卫不能整行返回（行内真标题连带丢失）；③模拟脚本须完整链模拟（4 星粘连形态 `**毒刺（****Poisonous` 不匹配行首分支，直接调 `_split_inline_star_titles` 不充分）+ 分进程（同进程两次 import 同名模块被 sys.modules 缓存）；④守卫必须验证触达（M13 教训重演验证：v2 词表守卫过度拦截靠全量 diff 捕获，单测试不产生 ≠ 链中不产生）

### M15 修复要点备忘（stat block 章节标题伪条目，回归链已走通）

- **形态与分布**：冒险之路AP_怪物 stat block 裸行章节标题（无 `**`，`---` 分隔形态）：`生态背景（Ecology）` / `特殊能力（Special Abilities）` / `栖息地与社会（Habitat & Society）`——`_wrap_bare_paren_colon_titles` 转星后 split L1576 词表判定未命中切伪条目；当前全库 23 个（生态背景 12 + 特殊能力 6 + 栖息地与社会 5），全在 AP_怪物；「阵营」排查时 45/52 在列但当前已无（随 M12 词表等顺带消掉），未补词防过度
- **根因**：`_MONSTER_BLOCK_SECTIONS = ("描述", "防御能力", "攻击能力", "数据", "生态", "习性")` 缺「生态背景」「特殊能力」「栖息地与社会」——带括号转星形态（`**生态背景（Ecology）**`）走 L1576 `name in _MONSTER_BLOCK_SECTIONS` 并入当前条目；词表缺词 → 切独立伪条目
- **修复**：`_MONSTER_BLOCK_SECTIONS` 补 3 词（生态背景/特殊能力/栖息地与社会）——最小改动，仅 L309 定义 + L1578 使用点；特殊能力带断行（`Special \nAbilities`）与双空格（`Special  Abilities`）形态均按中文名匹配覆盖
- **TDD**：簇 Z4（1 测试）——铜蛇 stat block 真实形态（`---` 分隔 + 裸行章节 + 能力列表）：断言 stat block 章节标题不切伪条目、生态背景并入铜蛇 text、带 Su/Ex 标记的能力条目（毒之瘴气/啃咬）独立切出（正确行为——初版断言「毒之瘴气并入铜蛇」是**错误断言**，能力列表条目本就该独立）、无孤立 `---` 分隔线 chunk
- **结果**：全量 pytest 768→**769**（簇 Z4）、chunk 3571→**3548**（-23 全定性：23 个 stat block 标题伪条目消失，生态背景 12 + 特殊能力 6 + 栖息地与社会 5）、verify_races 六检查全过（alt_trait 642、replaces 90.3%）、cicd 全链（pytest + pipeline + 验收 + 闸口）全过（空 text 0、>5000 1、book '?' ≤ 2450、toc 0）
- **diff 定性**（worktree 法，`/tmp/pf_agent_m15_baseline` 基线 8c61d93）：删除 31 唯一键（stat block 标题 18 键含 text='---' 特殊能力 6 去重 + 能力条目 13 键 text 尾部重排）、新增 13 键（同 13 个能力条目新 text——环境/组织/宝藏字段行从伪条目挪入前条目 text，无信息丢失）；唯一键差 18 vs chunk 净差 23 系 text='---' 多 chunk 同键去重所致，对账一致
- **登记不修**：并入后能力条目 text 尾部带 `---⏎**特殊能力（Special Abilities）**⏎---` 章节标题残渣（与描述/防御能力等章节标题并入先例一致，检索端可接受）；`---` 分隔线行残留 26 处归 M17（表格残留）范畴

### M16 修复要点备忘（霜巨人整章粘连，回归链已走通）

- **形态与分布**：怪物法典MC/霜巨人.md 30 行长连排（最长 3300 字符）——`新规则` 引言、专长/法术/物品章节标题（`霜巨人专长`/`霜巨人法术`/`霜巨人魔法物品`）与条目名全部嵌在行中，条目名以裸「中文 空格 EN（类型） 正文」流式形态同行连排（`…获得。有仇报仇 Ancestral Enmity（战斗）  你熟知…`），无 `**`、无换行；整章被吞成 1 个 monster_block 2830 字
- **根因**：`_RE_RACE_BARE_CN_EN_LINE` 原 `^` 行首锚定只处理行首裸条目——行中条目（章节引言后、字段链值尾）漏拆；且 `_repl` 只取括号分支正文（组4），无括号形态（`冰滑术 Ice Slick学派：…`）正文在组5 被吞（条目拆出但 text 为空，提取层丢弃——冰滑术曾「拆出但消失」）
- **修复**（`_wrap_bare_cn_en_lines_race`）：
  - 去 `^` 行首锚定 → 行中匹配；加 `(?<![一-鿿])` 前字符守卫（正文句引用 `使用有仇报仇 Ancestral Enmity 专长` 前汉字不拆）
  - `_repl` 输出前置 `\n`（标题独立成行，`_RE_TITLE` 行首锚定；前缀 `…获得。` 留在散文行并入前条目）
  - 组4/组5 双分支 tail（无括号形态正文不再被吞）
  - en 纯大写缩写守卫（`DC`/`AC`/`HD`…1-3 全大写，strip 后判断——组2 非贪婪会吞尾随空格）——`此豁免 DC 基于魅力` 不再拆伪条目（透肌魔人实测）
  - 撤销 INFIELD 变体：字段链行内「汉字前字符条目」实为句尾无标点紧贴（HTML 合并丢句号），与正文引用不可区分——冷酷逼视漏拆登记不修
- **源数据拆行**（3 处，LF 行尾）：`霜巨人专长`/`霜巨人法术`/`霜巨人魔法物品` 章节标题独立成行 → split 章节状态机识别 → 条目归正确 component_type（此前法术/物品全落 race_trait）
- **TDD**：簇 Z5（3 测试）——霜巨人 L5/L7/L9/L20/L24 真实形态：断言 9 条目拆出（有仇报仇/可怖冲撞/霜生种/冰寒石块/顺势扫荡/踏冰者/冰滑术/魔岩术/破雾目镜）、字段行并入前条目、类型断言（冰滑术/魔岩术→race_spell、破雾目镜→race_item、霜生种/踏冰者→race_feat——霜巨人专长章节归 feat 是正确行为）、正文引用不误拆、`此豁免 DC` 不误拆
- **结果**：全量 pytest 769→**772**（簇 Z5 3 测试）、chunk 3595→**3591**（净 -4：此豁免伪条目消除 1 + DC 类误拆 1 + 编号前移 2）、verify_races 六检查全过（alt_trait 643、replaces 90.4%）、cicd 全链全过（空 text 0、>5000 1、book '?' 2447 ≤ 2450、toc 0）
- **diff 定性**（worktree 法，`/tmp/pf_agent_m16_baseline` 基线 9eae24b）：chunk 净差 3548→3591（+43）；仅基线 0（零丢失）；仅当前 43 全定性——霜巨人 12 条目（feat 6 + spell 2 + item 4）+ 全库行中裸条目 31（feat/spell/item/trait 真条目）；30 个仅基线键 title 全部在当前存在（粘连块拆细：蒿兰人 1471 字 1 块 → 31 条目、新女巫巫术 580 → 化泥 560 + intro，片段法验证零丢失）
- **登记不修**：①冷酷逼视（`…战技攻击检定冷酷逼视 Icy Stare`——句尾词后无标点直连条目名，与正文引用形态不可区分，并入顺势扫荡 text 无损）；②翼龙人——闪避演习 text 3374 字吞并卡萨达人后续条目（基线 9eae24b 既有行为非本机制回归，归 M21 个例）；③`**削弱：**` 列表段标题（ISR 新种族「加强/调整/削弱」三段结构标题，基线既有行为，归 M21 个例评估）

### M17 修复要点备忘（表格残留家族收口，回归链已走通）

- **M17a 星壳错位（内海怪物志 4 文件，源数据修复 50 处）**：CHM→MD 转换器嵌套 `<STRONG>` 闭合错位产物——行尾 4 星（`X（EN）****`）、行中错位（`X（类型）**（EN`）、行尾孤星壳（`…正文**`）、半蝎人行中 4 星标题粘连（`X****Y（EN`）；修复脚本 `m17_fix_ismc.py`（幂等，每处替换前验证旧串唯一），修复形态 `**中文名（EN, 类型）**：正文`（类型并入 EN 括号逗号连接，`_split_paren_content` 字母段合并进 name_en 零代码改动）；4 文件 50 处 + stat block 行首空格防拆伪条目 1 处
- **M17b 兽态人 9 血统镜像表格**：`| 中文（EN）［血统名（EN）］　描述… |  |` 第一列有内容（与神裔/魔裔空第一列互为镜像），M6 `_RE_HERITAGE_TABLE_ROW` 不命中 → 整表 3198 字吞进「起始语言」；新增 `_RE_HERITAGE_TABLE_ROW_MIRROR` 拆出 `**血统名（EN）**` + `［中文名］　描述`（鲨鱼人后裔/熊人后裔等 9 血统独立，起始语言独立，stat block 章节标题保留）
- **KN123 裸 `---` 行剥除（`_strip_hr_lines` 公共层）**：CHM 转换 HTML `<hr>` 的 Markdown 形态，无语义（stat block 章节分隔/段落分隔）——race 源数据 110 行全进 chunk（39 chunks），剥成空行保留段落边界；带管道符表格分隔行 `| --- | --- |` 不命中（race `_RE_TABLE_SEP_ROW` 单独处理）；feat 180/法术 12/专长 407 残留已收口（已冻结模块不修），调用方按类目启用
- **M17c H2 吞并前缀守卫**：`---` 剥除暴露 M9 缺陷——H2 主标题（空 text）后「首个标题行并入作正文」把子条目标题吞进壳（旧库靠 `---` 行侥幸填充 H2 text 未暴露）：BotC 替换儿亚种 H2 后妖鬼婆裔替换儿被吞（9 替换儿剩 8）、PHH 元素裔亚种 H2 后宝石之魂被吞（8 魂系剩 7）
  - **组4 判据不可用**：normalize 已把 `**X（EN）**：正文` 拆成标题行+正文行（`_split_closed_title_colon` 公共层，feat/spell 同构），split 时组4 恒空
  - **前缀判据**：H2 名是标题行名前缀才并入（ISR 勒珊塔灵族 → `**勒珊塔灵族角色（…）**` 简介标题 ✓ 并入；BotC 妖鬼婆裔替换儿 / PHH 宝石之魂 ✗ 独立切出）——三场景一判据全区分，零特判
  - 行为变化：BotC 替换儿亚种 / PHH 元素裔亚种章节壳被 dedup 弹掉（壳无自身正文，零信息丢失），10/8 子条目全部独立
- **结果**：pytest 784（M17c 2 测试 + 簇 R 5 + M17b 6，零回归）、chunk 3612、verify_races 六检查全过（alt_trait 643、replaces 90.4%）；diff 定性（对比 M17 开工前备份 3591）：52 丢失全定性——50 内容保留（M17a/b 修复形态）、1 零信息壳（替换儿亚种 text=`---`）、1 拆分重组（半人马陷阵士 → 陷阵士 7 条目）；新增 73 全合理（兽态人 9 血统 + BotC 10 替换儿 + stat block 修复形态）

### 批次 3 进度（2026-08-04）

| # | 状态 | 结果 |
|---|---|---|
| M18 | ✅ 已修复 | 源数据删除 69 处（32 文件）+ 14 保留项；pytest 784、chunk 3612（不变）、verify 六检查全过、chunk 真残留 0（详见 M18 备忘节）；KN119 闭环 |
| M19 | ✅ 已修复 | 源数据修正 2 处：page_10 灵猴族「攀爬30尺」→「攀爬20尺」+ page_440 土元素裔漏「裔」字；verify 基线同步（详见 M19 备忘节） |
| M20 | ✅ 已修复 | 范围核查确认非缺口：源数据标题整理 → 产出 race_intro（详见 M20 备忘节） |
| M21 | ✅ 已修复 | KN120 个例收尾：海威专长章节误判 + 备注词表 + 神裔奇星壳（详见 M21 备忘节）；KN120 部分闭环 |

### M18 修复要点备忘（译者吐槽/译注删除，KN119 闭环）

- **取舍标准**（KN119 处置口径「保留合法规则注释」）：删吐槽/情绪/第一人称口水、翻译元信息（译名选择、翻译说明、查无此/存疑/原文如此/勘误/求解、见下文导航、论坛外链译注）、与正文矛盾的猜测；保留规则数据/术语引用/规则解读
- **删除 69 处**（32 文件）：吐槽 16（地精/大地精/半身人/半精灵/精灵/猫族/鼠族/土元素裔/BotN 3/兽态人 3/人类汇总 1）、译注编注口水 50+（半兽人 8、狗头人 4、兽人 3、地精 2、人类 2、侏儒 2、page_1366 2、卓尔 page_164 论坛外链 2、page_163 恶魔城/豁免质疑 2、莱西 2、page_18 海贼 3、神裔宁静词汇 1、page_442 2、page_815/816/940 各 1、page_27 1、page_324 1、page_1456 1、冒险之路AP 1、page_129 1）、速查块尾部沙包口水 1（page_163 `【速查：吸血鬼弱点，译者**沙包**】` → `【速查：吸血鬼弱点】`，速查主体保留）
- **保留 14 处**（合法规则注释）：page_129 次要天生武器 BAB / 先知秘示域奖励法术、page_160 主要/次要天生武器 + 用毒职业列表、page_165 侏儒精类起源、page_167 精制镣铐 DC35/DC28、page_17 凶猛为兽人特性半兽人没有 + 凶暴决意满足先决条件、page_205 体质+2 每级多负2点血、page_238 瘟疫之瓶取代突变药剂 + 次要天生武器、page_402 类人生物子类术语、page_403 小型1d3中型1d4 + 潜行时机、ISR 其他 Xotani CR20 魔法兽、矮人汇总 工匠 PFS 不开、page_1456 整轮动作、page_441 三神名+阵营数据（尾句「整体上相当平衡嗯」轻微口水，含量小不拆）
- **行尾教训（pf-organized-source-crlf-convention 复现）**：首版脚本 `read_text/write_text` 把 27 个 CRLF 文件全转 LF（git diff 全文件空行污染）；改**二进制读写**（read_bytes/write_bytes + UTF-8 编码替换串 + 跨行串适配 `\r\n`）后 CRLF 原样保留，diff 只剩精确删除（32 files, 64 insertions, 70 deletions）
- **结果**：pytest 784 全过、pipeline 3612 chunks（结构零变化，纯文本删除）、verify_races 六检查全过（alt_trait 643、replaces 90.4%）、chunk text 已删模式真残留 0（18 处正则命中全部可归因为保留项/玛格尼玛「尼玛」子串误报）、保留项抽查全在 chunk 中（page_402/403 不进 race pipeline 属既有行为，源数据保留已确认）


### M19 修复要点备忘（灵猴族攀爬速度源数据修正，KN117 闭环）

- **处置**（KN117）：page_10 汇总表灵猴族「30尺，攀爬30尺」→「30尺，攀爬20尺」（page_379 详情页 + 官方 ARG 均为 climb 20 ft）；顺带修复 page_440 土元素裔「缓慢速度」漏「裔」字（全文件唯一裸写「土元素」，其余 50+ 处均「土元素裔」）
- **verify 基线同步**：「3/6 六字段规范化比对」基线 `vectorizer/exploration/race/audit_baseline.jsonl` 灵猴族 speed 为旧错误值（30尺），同步修正为 20 尺——基线本身是错误数据，属「测试断言业务错误则修正测试」条款
- **残留核查**：全库「攀爬30尺」仅剩 1 处 = page_13 树之歌者「树人幼苗（Sapling Treant）植物伙伴」速度（官方树人 30ft+climb30ft，合法数据非残留）
- **回归链全绿**：pytest 784 → pipeline 3612 chunks（结构零变化）→ verify_races 六检查全过 → race_page_10_0034 灵猴族 race_overview「速度：30尺，攀爬20尺」✓


### M20 修复要点备忘（page_812 阿斯托莫伊人范围核查 + 源数据整理，KN099 局部关闭）

- **范围核查**（audit_chm_scope_gap.py）：种族 CHM 68 页 / 覆盖 68 / 缺口 0 → 非输入缺口；page_812 为「未整理页格式」（裸两行标题 `阿斯托莫伊人⏎astomoi这些类人像…` 无加粗标记）导致解析器零产出 → **解析缺口（源数据格式）**，KN118 同定性流程
- **修复**（m20_fix_astomoai.py，二进制 2 处替换）：L7 裸中文标题 → `**阿斯托莫伊人（****Astomoi****）**`（保留 CRLF）；L8 行首英文残片 `astomoi` 删除（英文名并入标题）
- **产出**：race_page_812_0000 race_intro「阿斯托莫伊人」874 字符，8 个特性（属性调整/中等体型/标准速度/灵敏嗅觉/感应视野/敏感呼吸/无嘴/起始语言）全部并入正文，text 无 `\r`/英文残片残留，断行（Telepathic Senses 等）被 normalize 修复
- **豁免移除**：verify_races `_EXEMPT_DOCS` 移除 page_812（保留 BoS 影生暗生聚合排除）；判别器回归 est=1 got=1 对账通过、静默丢失 0
- **回归链全绿**：pytest 784 → pipeline 3613 chunks（+1 全定性：race_intro 102→103，其余 10 类零变化）→ verify_races 六检查全过
- **维持豁免**：BoS 影生暗生（聚合文件内容重复）、page_401~404（种族构建规则，另立任务不阻断）；阿斯托莫伊人特性粒度拆分（8 特性独立 chunk）另立任务


### M21 修复要点备忘（KN120 个例收尾：海威专长章节误判 + 备注词表 + 神裔奇星壳）

- **海威专长章节误判**（KN120 人鱼「深水哨卫标题与正文错位」根因，最严重项）：`_match_section` 章节关键字「专长」是**子串匹配**——archetype 能力段「**海威专长（Aquatic Prowess Feat, Ex）**」组1 含「专长」被误判 race_feat 章节：①章节行不产条目 → 海威专长标题丢失、正文并入深水哨卫（chunk len 355）；②state 从 race_archetype 永久切到 race_feat → 偏好地形/海之子/水族召唤 3 能力段分类污染。**修复**：`_match_section` 加能力段形态守卫——`_RE_TITLE` 组2 以 `, Ex|Su|Sp` 结尾 → 非章节标题（与 L1655 race_spell 退态逻辑同款既有口径；真实章节标题括号内无能力标记，零误伤）。**全库扫描确认受影响标题仅 2 处**：page_323 海威专长（本次修复）+ page_165 类法术能力（race_spell 形态校验既有挡住，无变化）
- **结果**：波涛守卫 5 能力段全独立切出、全 race_archetype（海威专长 len 310 标题保留、深水哨卫净化 355→44、偏好地形/海之子/水族召唤 race_feat→race_archetype）；与 L1699「archetype 能力独立切出」既有设计（page_160 变体能力丢失教训）完全一致
- **备注词表**（半兽人 page_17）：`_ENTRY_FLOW_FIELD_LABELS` 加「备注」——狂暴专长字段链末尾 `**备注**：如果该伤害仍会使你失去意识…` 原切 title=备注 的 race_feat 伪条目（len 217）→ 并入不屈凶暴（len 556）；全库 title=备注 残留 0、仅 1 处并入（精确修复面）
- **神裔奇星壳断裂**（KN120 神裔项）：page_129 L685 `**神裔奇****物（****Aasimar**** Magic Items****）******`（HTML 加粗拆裂）→ `**神裔奇物（****Aasimar Magic Items****）******`（m21_fix_aasimar.py 二进制幂等，CRLF 277 不变）；伪条目「神裔奇」消失 + 章节关键字「魔法物品」恢复识别 → 天界之盾/净土之盾/威吓之环 race_feat→race_item（3 处）
- **误报澄清**：娜迦裔催眠凝视「2d4的生物群陷入迷魂状态」截断为查看截断误报（完整 text 含该句）；速查块嵌入条目正文是既有设计，登记不修
- **登记不修**：强壮尾部 replaces 空（源数据无取代说明）、精灵特殊材料「价格调整」表格行（内容合法）、树人幼苗攀爬30尺（官方数据）、阿斯托莫伊人 8 特性粒度拆分（另立任务）
- **回归链全绿**：TDD 2 测试（`test_archetype_ability_with_feat_word_in_name` + `test_note_field_line_merged_in_feat_flow`）红→绿 → pytest 786 → pipeline 3612 chunks（-1：神裔奇伪条目；item +3 / feat -4 / archetype +4 全定性）→ verify_races 六检查全过（37 召回 100%、六字段全一致、replaces 90.4%）→ diff 定性（page_323 5 能力段 / page_17 备注 / page_129 奇物章节）
- **批次 3 全部闭环**：M18（KN119）→ M19（KN117）→ M20（KN099 局部）→ M21（KN120 部分）

### M22 修复要点备忘（KN116 审计残留：ISR×2 + BotC 替换特性系统性误标，2026-08-04）

- **背景**：全量核查收尾审计复查 KN116 状态时发现——ISR 其他（45 条）/ISR 核心（52 条）/BotC（5 条）替换特性文件共 **102 条 race_trait 系统性误标**（替换特性应为 alt_trait；M9 声明「ISR 其他 4」与现状不符，点名实例「大地精——权威性」仍在 race_trait）
- **根因三层**：
  1. **H2 章节判定缺失**：整理规范 H2（`## 内海种族 ISR 其他种族替换特性`）带 `## ` 前缀不匹配 `_RE_TITLE`/`_RE_BARE_CN_TITLE` → `_match_section` 不触发 → 整文件落默认态（intro）→ 条目 race_trait。修复：H2 行含章节关键字时包星壳 `**{标题}**` 走章节判定（全库仅 6 处 H2 含章节词：种族特性×4/替换特性×1/天赋职业×1，命中即切态不产条目；不含关键字的 H2 保持原行落散文吸收）
  2. **alt_trait 关键字缺口**：`("alt_trait", ["特性替换"])` 只覆盖「种族特性替换」排列——ISR 形态「种族替换特性」（内海种族 ISR 其他种族**替换特性**）不含「特性替换」子串（顺序相反）；BotC「替换种族特性」被「种族特性」误收进 race_trait（在 race_trait 关键字之前无 alt_trait 命中）。修复：三排列全覆盖 `["特性替换", "种族替换特性", "替换种族特性"]`（第三者须先于 race_trait「种族特性」命中——遍历顺序保证）
  3. **pending_race_h2 信号未消费**：`<!-- ISR-source:page_1326.md:其他种族替换特性 -->` 文件级注释被 `_RE_SOURCE_COMMENT` 匹配置 pending_race_h2（M9 逐种族注释信号语义），H2 章节切态后信号保留 → 下一条目「其他种族」裸中文标题消费信号 → 切 race_intro + state 重置 intro → 后续条目仍 race_trait。修复：章节判定命中时 `pending_race_h2 = False`（章节标题优先于种族 H2 信号——含章节关键字的行不可能是种族 H2）
- **结果**：ISR 其他 45→**46** alt_trait、ISR 核心 52→**59** alt_trait（+7 组标题「矮人/精灵/侏儒/半精灵/半兽人/半身人/人类」+「其他种族」sidebar→alt_trait——组说明「X 角色可以选择以下种族特性…」归 alt_trait 家族合理，内容零变化）、BotC 5→5 alt_trait、page_939/940 顺带 4 条归位（「种族替换特性」关键字扩展命中）；总数 **3612 零丢失**；TDD 3 测试（H2 切态 / H2 回归 / 信号优先）红→绿 → pytest 786→**789** → verify_races 六检查全过（replaces 89.7%，ISR 句内「此种族特性替换X」replaces 提取生效）→ cicd 闸口全过
- **残留登记（审计发现，P3）**：4 个孤立文件 race_name 非官方种族名——`嗜火者`（CtIE 替换儿选项文件）/`边栏：非人类神裔`/`边栏：非人类魔裔`（独立边栏文件）/`兽态人Skinwalker种族特性`（page_806）——内容全部合法完整、语义检索可命中，仅按 race_name 精确过滤时漏检；`年龄`×2 为 KN113 设计保留（正确）。**KN133 已修复 `嗜火者`**（2026-08-04，a7dbdb3：CtIE 源数据缩写紧贴加空格后 `_wrap_bare_cn_en_lines_race` 转星切 intro → 嗜火者升级 race_trait、race_name 污染消失），残留 4→3
- **相关澄清**：半身人「不幸半身人/半身人霉咒」title 拼组为**源数据合法形态**（同义译名 `/` 并列，EN=Unlucky Halfling），非解析 bug，登记不修

### 验收返修备忘（k3 验收报告 KN130~133，2026-08-04，commit `ca4f26e`/`a7dbdb3`）

- **KN130（P1）**：page_11 汇总页三族整行静默丢失（矮人/神裔/替换儿）——源表 37 族 × 2 表对账后找回，chunk 70→76（74 overview = 37 族 × 2 表）；簇 I 测试（年龄表 9 格「岁」/身高表 9 格「寸」行解析）
- **KN131（P1）**：page_752 52 条误标 race_trait → 59 条全 alt_trait（+7 组标题）+ 全库同类漏网排查 page_398 35→42 一并修复；book '?' 设计保留同口径（源文件无「> 来源：」块、多来源无法单一归书）
- **KN132（P1）**：race_name 四级兜底链（intro 登记 > 路径归属 > 「X——Y」拆分 > title 兜底）+ 书名/章节 intro 黑名单豁免；13 测试
- **KN133（P2）**：边界/清洗残留家族（剥粘表/剥「：」/合劈半三形态 + 译注 M18 同模式复扫清零）+ CtIE 嗜火者 intro 吞并（源数据缩写紧贴修复，嗜火者 race_intro→race_trait、race_name 污染消失）
- **验收建议 5**：「page_11 行级召回」「page_752 component_type」补入 verify_races（六→八检查），TDD 先行（test_race_verify_checks.py 10 用例）
- **回归链终态**：pytest 789→**836** → pipeline **3654 chunks**（3612+42 全定性：KN130 +6 / KN131 page_398 +7 / KN133 page_806 +9 等）→ verify_races 八检查全过 → 闸口全过（空 text 0、>5000 1/3、book '?' 2483/2483、toc 0）
- **行尾事故记录**：page_806/page_26 源数据修复曾误转 CRLF 行尾（恢复 HEAD 重做，diff 仅预期内容变化）——教训：源数据修改一律字节级 + 逐字节核对行尾分布
