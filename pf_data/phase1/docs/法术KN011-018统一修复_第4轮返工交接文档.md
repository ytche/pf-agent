# 法术 KN011~KN018 统一修复 — 第 4 轮返工交接文档

> 撰写：k3（方案设计方 + 审计方），2026-07-30
> 执行：执行模型（整包交付，护栏内自主决策）
> 配套：[第 3 轮审计报告](法术KN011-018统一修复_审计报告_k3_第3轮.md)（返工依据，F3-a~F3-d 事实链全在其中） | [审计要求](法术KN011-018统一修复_审计要求.md) | [第 3 轮交付文档](法术KN011-018统一修复_第3轮交付审计文档.md)
> 基线 commit：`1aef433`（第 3 轮审计提交），branch `fix/kn011-018-unified`

---

## 0. 当前状态一句话

第 3 轮审计：**代码主体修复真实且高质量**（S1~S6 绝大部分属实验收），不验收的原因是 **1 个数据回归（KN011 链接丢失）+ 3 个文档/登记问题**。本轮 T1~T4 **不动任何解析器代码、不改 chunk 边界**，工作量集中在"一条命令 + 文档更正 + 两个 KN 登记"。

---

## 1. 返工项总览

| # | 内容 | 性质 | 预估 |
|---|---|---|---|
| T1 | 重跑 `link_mythic_base_spells.py` 恢复 MA 253/253 + 固化后处理链 | 一条命令 + 固化 | 小 |
| T2 | 第 3 轮交付文档 4 处更正 | 文档 | 小 |
| T3 | KN013 豁免清单 + 正式立项 KN020（只立项不修） | 文档 + 立项 | 中（豁免清单逐项） |
| T4 | KN019 census 更正 + D 类逐条登记 | 文档 | 中（D 类 ~20 逐条） |

---

## 2. T1：恢复 KN011 链接 + 固化后处理链

### 2.1 根因（k3 已定位，附重放证据）

KN011 的 `base_spell_chunk_id` 字段**不是向量化流水线产物**，而是独立后处理脚本 `pf_data/phase1/link_mythic_base_spells.py`（commit `0501dc6`）事后写入 chunks.jsonl 的。第 3 轮重生成覆盖了 chunks.jsonl，只重跑了 S6 的 VC post-process，**漏跑了 link 脚本** → 交付物中 253 个 MA 增强 chunk 的链接字段整体丢失（0/253）。

k3 已在 /tmp 副本上重放验证：

```bash
cd pf_data/phase1
cp vectorizer/output/法术/chunks.jsonl /tmp/chunks_r3.jsonl
python3 link_mythic_base_spells.py --input /tmp/chunks_r3.jsonl --output /tmp/chunks_r3_linked.jsonl
# 实测输出: Match rate: 253/253 = 100.0%，血沸 spell_page_625_0034 → spell_Spell UM_0028，总数 4200 不变
```

### 2.2 执行内容

1. **重跑**：`python3 link_mythic_base_spells.py`（默认原地更新 `vectorizer/output/法术/chunks.jsonl`）。
2. **固化后处理链**：漏跑的结构性原因是"重生成后要手工记得跑哪些后处理"没有固定入口。二选一（执行方自主决定）：
   - **方案 A（推荐）**：写 `pf_data/phase1/regen_spell_chunks.sh`（或 .py），把 `向量化重生成 → link_mythic_base_spells.py →（如有其他 post-process）→ verify 三件套` 串成一个入口，并在 WORKFLOW.md 或 vectorizer/README 中明文"重生成后必须走此入口"；
   - **方案 B**：不写脚本，但在 WORKFLOW.md 新增"法术重生成检查清单"一节，逐条列出后处理步骤与验收命令。
3. **自验**：`verify_kn011_018.py` 中 "MA增强chunk匹配量" 从 0 回到 **253**；"完整MA法术带base_id=0"、"base_id指向MA=0"、"base_id指向不存在chunk=0" 三项保持 ✅。

### 2.3 验收口径

| 指标 | 当前 | 目标 |
|---|---:|---:|
| MA 增强 chunk 带 base_id | 0/253 | **253/253** |
| 血沸链接 | 无 | `spell_page_625_0034 → spell_Spell UM_0028` |
| chunk 总数 | 4200 | **4200 不变** |
| pytest | 264 | 264 全绿（T1 不改代码，不得掉测试） |

---

## 3. T2：第 3 轮交付文档 4 处更正

更正对象：`法术KN011-018统一修复_第3轮交付审计文档.md`（在原文上修订并标注"第 4 轮更正"，或在第 4 轮交付文档中设专节如实更正——执行方自主选择形式，但必须可复算）。

| # | 位置 | 失实内容 | 更正为（k3 实测，可直接引用） |
|---|---|---|---|
| 1 | §10 KN011 表 + §9.1 | "253/253 (100%)"、"实际匹配仍为 253/253"、"范围外本轮不动" | 交付物实际 **0/253**（重生成漏跑 link 脚本所致的功能回退，非"范围外"）；T1 修复后 253/253，附复算命令 |
| 2 | §3.4 验收指标表 | 缺交接 §2.3 的"干净→脏回归 160→0"整行 | 补行：回归 160→**56**（修复 104），附 56 清单的生成命令与形态分析（见 §4 数据）；M1 201→109 是实质进展但未达交接目标 0 |
| 3 | §9.1 M1 归因 | "KN013 + KN019 同根因" | 归因更正：85 个 M1 脏 chunk 与 47 个 KN019 伪标题 chunk **交集为 0**；残留形态为**悬挂 `**` 33 + 行内无换行粘连 37 + 其他**，归 KN020（见 T3）；核心书 31/56 |
| 4 | §10 KN016 "+1 增量来源待查（KN019 散文碎片误识别）" | 猜测错误 | 第 12 个多学派 chunk = `spell_page_1365_0480`，S2 源数据归一化补 `**学派：**` 标签的**良性副作用**，非新合并污染；同时把 verify 中 N1 注释 "11(AP…)" 更新为 12（11 AP + 1 COMP/page_1365_0480） |

---

## 4. T3：KN013 豁免清单 + KN020 立项

### 4.1 豁免清单（交接 S3.3 的程序要求，第 3 轮漏交）

对 KN013 未达标的 4 项指标，**逐项**列出 chunk_id + 值 + 定性（合法长值 / 真吞正文），合法长值申请书面豁免，真吞的编入 KN020。k3 已抽样定性，执行方需补全量：

| 指标 | 实测 | k3 抽样定性 |
|---|---:|---|
| effect len>40 | 28（含 MA 口径；非 MA 27） | **多数合法**：`ACG_0127`/`CRB_0045`/`CRB_0508`/`CRB_0615`/`OA_0143`（墙类尺寸枚举）、`CRB_0396`（召唤 HD 限制）、`CRB_0411`（法术名列举）；**真吞例**：`UM_0020`（值为正文片段"－近战：如果你们同时攻击…"） |
| target len>40 | 16 | 未抽样，执行方全量定性 |
| duration 含 `。` | 26 | **多数真吞**：`OA_0043/0045/0115`（`...(可解消)**你让自己陷入…`）、`UI_0022/0040/0087`（无分隔直接粘散文）——超出当前 `_PROSE_LEAD_INS` 信号集 |
| duration len>40 | 19 | 与上一项高度重叠，一并定性 |

### 4.2 KN020 立项（写入 `docs/已知问题记录.md`，只立项不修）

**KN020. 字段值残留三形态：散文真吞 / 悬挂 `**` / 行内无换行粘连（~100 chunks，登记不修）**

| 形态簇 | 规模（k3 实测） | 实样 | 修复方向（登记用） |
|---|---:|---|---|
| ① duration 真吞散文 | 26 | `OA_0043` `'1小时/等级 (可解消)**你让自己陷入…'`；`UI_0022` `'10分钟/等级当你施放该法术时…'` | 扩 `_PROSE_LEAD_INS` 信号集（"当你/你让/你的"等引导词）+ 无分隔粘附场景处理 |
| ② 悬挂 `**` 残留 | 33 | `APG_0082` `'10分钟/等级或 **'`；`OA_0084/0085` `'10分钟/等级 (可解消)**'`；`UM_0179` `'立即 **\n或 1轮'` | `_truncate_value_prose` 尾部孤儿 `**` 剥离（注意保护 `**接触**` 类合法包裹） |
| ③ 行内无换行粘连 | 37 | `AG-冒险者指南_0017` range `'近距 (25尺+5尺/2级)**区域：** …'`；`MC-怪物志_0003` range `'接触**法术'` | Bug A 修复只覆盖 `\n` 分隔形态；无换行形态需在 KN012 拆行层或截断层扩展 |

- 三簇合计 85 个脏 chunk（有重叠），与 KN019 伪标题群体**交集为 0**（审计已证实，立项时写明防止再次错误归因）；
- 核心书占比：56 回归中 31 个核心书（APG/UM/UC/CRB）；
- 关联：[[KN012]]（拆行是 ②③ 的上游）、[[KN013]]（① 是其残留）。

### 4.3 验收口径

- 豁免清单逐项完整（4 项指标全覆盖，每项 = chunk_id + 值摘录 + 定性 + 理由）；
- KN020 章节含三形态的规模、实样、chunk_id 清单（或清单生成命令）、修复方向；
- 本轮**不修** KN020，verify_kn011_018 中这 6 项（KN013 四项 + M1）允许继续 ❌，但交付文档必须写明"已登记 KN020 + 豁免清单编号"。

---

## 5. T4：KN019 census 更正 + D 类登记

### 5.1 census 更正（k3 独立扫查结果，直接采用）

| 子类 | 原登记 | 更正为 | 依据 |
|---|---:|---:|---|
| A 罗马数字 | 44 | **44**（不变，登记准确） | doc 分布逐项吻合 |
| B 散文碎片 | 2（两处文档自相矛盾：§5 写 APG_0176+ARG_0009，已知问题记录实样写 APG_0156） | **3**：`spell_Spell APG_0156`、`spell_Spell APG_0176`、`spell_Spell ARG_0009` | 三个 chunk 的 title 均为散文句，k3 已逐一核对 |
| C 字段值标题 | 1（page_625_0022） | **2**：补交接点名的 `spell_page_1219_0013`（'一个40立方英尺/等级'）；`spell_page_625_0022`（'野兽形态 全部等级'）**可裁定保留为合法聚合标题**，但须写明裁定理由（MA 全等级聚合 chunk，标题有描述性）——若裁定保留则 C=1 + 豁免 1 |
| **D 续段散文标题（新增子类）** | 未登记 | **~20**，逐条登记 | 见 §5.2 |

### 5.2 D 类：续段散文标题（本轮审计新发现，三轮共同盲区）

**形态**：法术 stat block 单独成 chunk（无正文），正文段落因源数据段首加粗被 split 成独立 chunk，标题 = 正文首句。历史快照比对确认**第 1 轮备份（4470）中已存在**，非任何一轮的回归。

**k3 确证清单（20 条，执行方逐条复核后登记）**：

```
CRB×9: spell_Spell CRB_0027 / 0032 / 0373 / 0483 / 0502 / 0546 / 0556 / 0592
       （0546、0556 标题同为"召唤来的怪物无法召唤…"，分属不同召唤怪物续段）
UM×8:  spell_Spell UM_0048 / 0050 / 0052 / 0054 / 0075 / 0153 / 0217 / 0239
APG×2: spell_Spell APG_0268（APG_0156 已计 B 类，不重复登记）
其他:  spell_page_1177_0000、spell_page_1365_0191
```

**边界候选（5 条，执行方裁定，倾向合法）**：`APG_0042`（'破除结界、复原术'枚举标题）、`APG_0242`（集群枚举）、`ISG_0002`、`ISG_0017`（中文+英文合法标题）、`page_805_0007`（含法术名'融为一体'但格式脏——可单列为"标题格式脏"注记）。

**典型实样**（写入 KN019 章节）：

```
spell_Spell CRB_0372  坐骑术                              ← 仅 stat block（177 字符，无正文）
spell_Spell CRB_0373  你召唤出一匹轻型马 (light horse)…   ← 坐骑术的正文，标题=正文首句
```

**影响定性（写入 KN019）**：D 类是 KN019 各子类中**对问答机器人最严重**的一类——按法术名检索只能命中无正文的 stat block chunk，正文 chunk 与法术名失联（与第 1 轮"共用召唤坐骑空壳"同款），且集中在 CRB/UM 核心书。**修复优先级应高于 A 类罗马数字**，KN019 章节的"修复方向"一节需相应调整顺序。

### 5.3 其他登记修正

1. 44 个罗马数字 chunk_id 清单**落仓库**（当前引用 `/tmp/k019_roman_ids.txt` 易失文件）——可附在 KN019 章节或 `docs/state/` 下；
2. N1 的 11 个 AP/补充多学派 chunk 并入 KN019 登记（交接第 3 轮 §5 已要求，第 3 轮漏做）；
3. KN019 章节头部的总数、占比、来源分布按更正后 census 重算（约 44+3+2+20 ≈ **69**，占 4200 的 ~1.6%）。

---

## 6. 红线（护栏）

1. **不动解析器代码**：T1~T4 全部不涉及 `vectorizer/` 下任何 `.py` 修改（KN019/KN020 修复方向只登记）；唯一允许的代码侧产物是 T1 的固化入口脚本（新文件，不改既有逻辑）。
2. **chunk 总数 4200 不变**：T1 的 link 脚本只加字段不增删 chunk；若执行后发现总数变化，立即停下来查原因。
3. **KN019/KN020 本轮只登记不修**：避免范围蔓延，修复排入下一轮解析器迭代。
4. **文档数据可复算**：凡标 ✅/数值的结论附复算命令；禁止口径偷换（三轮不验收的共同主因）；合法长值豁免必须逐项，禁止整批推给"KN020+"了事。
5. **Git 安全**：提交前 `git status` 确认只动预期文件；任何丢弃性命令前先 `git stash`。

## 7. 交付要求与验收流程

1. **自测**：T1 后 verify_kn011_018 中 KN011 四项全绿（匹配量 253）；pytest 264 全绿不掉；verify_spells 全通过；chunk 总数 4200。
2. **第 4 轮交付文档**：逐项给修复前/后数值 + 复算命令；T2 四处更正在文中显眼位置；KN013 豁免清单与 KN019 census 表全文附上。
3. **k3 第 4 轮审计**：独立重算（MA 链接、回归 diff、KN019/KN020 登记完整性抽查）。

## 8. 复跑入口

```bash
cd /Users/chezi/code/java/pf_agent/pf_data/phase1

# T1 恢复 MA 链接
python3 link_mythic_base_spells.py

# 验证三件套
python3 -m pytest vectorizer/tests/ -q
PYTHONPATH=. python3 vectorizer/verify/verify_spells.py
PYTHONPATH=. python3 vectorizer/verify/verify_kn011_018.py

# T1 验收：MA 链接（期望 253）
python3 -c "
import json
chunks=[json.loads(l) for l in open('vectorizer/output/法术/chunks.jsonl',encoding='utf-8') if l.strip()]
ma=[c for c in chunks if c.get('book_abbreviation')=='MA']
print('MA:',len(ma),'带base_id:',sum(1 for c in ma if c.get('base_spell_chunk_id')))"

# T2 验收：回归 diff（期望报告 56 的清单；本命令生成清单）
python3 -c "
import json
load=lambda p:{c['chunk_id']:c for c in (json.loads(l) for l in open(p,encoding='utf-8') if l.strip())}
cur,base=load('vectorizer/output/法术/chunks.jsonl'),load('vectorizer/output/法术/chunks.jsonl.kn011-backup')
F=('duration','target','effect','school','casting_time','range','area','spell_level')
def m1(c):
    m=c.get('metadata') or {}; n=0
    for f in F:
        v=m.get(f)
        if isinstance(v,str): n+=v.count('**')
        elif isinstance(v,dict):
            vv=v.get('value','')
            if isinstance(vv,str): n+=vv.count('**')
    return n
reg=sorted(k for k,c in cur.items() if k in base and c.get('book_abbreviation')!='MA' and m1(base[k])==0 and m1(c)>0)
print('干净→脏回归:',len(reg)); [print(' ',k) for k in reg]"

# T4 验收：D 类候选标题扫查（宽启发式，供逐条复核）
python3 -c "
import json,re
chunks=[json.loads(l) for l in open('vectorizer/output/法术/chunks.jsonl',encoding='utf-8') if l.strip()]
for c in chunks:
    t=(c.get('title') or '').strip()
    if re.search(r'[。；]',t) or t.startswith(('你','该法术','这法术','此法术','除了','那些','如果','当')) or len(t)>22:
        print(c['chunk_id'],'|',t[:45])"
```
