# 流程教训沉淀（专长模块全流程复盘）

> 记录时间：2026-08-03（专长模块阶段收口 + CI/CD Phase 1 落地后）；2026-08-04 追加 E5（种族模块二轮审计后人工抽查暴露的输入范围缺口）
> 来源：Prepare Phase 三轮返工 → TDD 五轮审计 → KN076~095 全闭环 → KN095 修复 → CI 落地全程复盘；E5 来源 KN107~109
> 用途：**后续模块（装备/怪物/Java 后端）开工前必读**；处理数据前按需查对应条目
> 经验链：专长本文件 → 种族 `跨模块经验沉淀_种族模块_20260805.md` → trait `跨模块经验沉淀_背景特性模块_20260806.md`（第三条，2026-08-06），下一模块三条合并使用
> 配套：`WORKFLOW.md`（V/N 迭代流程）、`专长模块开工前_流程抽象与预防清单.md`（开工前预防）、`已知问题记录.md`（KN 全量）

## 一、环境与工具类（CI 落地暴露）

| # | 问题 | 沉淀机制 | 后续参考 |
|---|---|---|---|
| A1 | 本地 Python 3.14 的 PEP 649 惰性注解掩盖未导入类型（pipeline.py `FileProcessor`），CI 3.12 才 NameError | 注解引用名必须显式导入；全局 ast 扫描一次性排查（扫描脚本见 CI 提交 `1b17dc1`） | 新模块开发前跑 ast 注解检查；CI 固定 Python 3.12 |
| A2 | gitignored 产物（chunks.jsonl）CI checkout 拿不到，产物级测试 FileNotFoundError | 产物级测试加环境守卫（缺失时 `pytest.skip`），本地产物齐全仍全量执行 | 后续模块产物级测试一律带守卫；产物断言职责归 pipeline+verify 步骤 |
| A3 | 无 CI 时手动验证靠自觉，回归晚发现 | 质量闸口（`.github/workflows/ci.yml`）：pytest → pipeline 重跑 → 四后处理 → verify → 不变量 → 产物归档 | 新模块直接并入闸口，不变量脚本按模块扩展 |

## 二、数据回填与链路类（重跑回归重灾区）

| # | 问题 | 沉淀机制 | 后续参考 |
|---|---|---|---|
| B1 | 重跑 pipeline **重置后处理产物**（book/source/toc 全回退）；原方案只记两个 apply 脚本，遗漏 backfill 两脚本 → source 993→337、toc 回退 | **已根治（v2.2 决策 A，2026-08-03）**：四脚本迁入 pipeline finalize 阶段（`FeatProcessor.POSTPROCESSORS` 类 + run()，`vectorizer/postprocess/feat_chain.py`），重跑即自动执行——「编排保证链路」升级为「结构保证」；法术 link_mythic 为冻结模块保持外挂（§8 债表） | 任何 pipeline 重跑自动含后处理链，无需查脚本清单；后续模块直接声明 POSTPROCESSORS |
| B2 | 判别器矩阵 rel 是**子目录版路径**（`专长/xxx.md`），首轮修复误改根目录副本，pipeline 重跑毫无变化 | fix 脚本 FILES 指向子目录版；根目录副本 git checkout 回滚 | 改源数据前先确认矩阵 rel 指向的版本（子目录 vs 根目录副本） |
| B3 | 映射表是后处理脚本（非 pipeline 内建），pipeline 重跑后不重跑即回归（book 未知 1780→1806 二度回归） | 映射表内嵌脚本（apply_chm_toc_mapping.py），映射成果以文档登记 | 映射类修复尽量内嵌脚本，避免依赖外部文件 |
| B4 | 验证链双份编排：regen 脚本与 CI yml 各写一份验证链，验收面漂移（regen 244 vs CI 530；regen Step 2 占位 2 天回归窗口实证） | **已根治（v2.2 决策 B，2026-08-03）**：收敛为单一命令 `python3 -m vectorizer.cicd verify --category feat`（cicd 包 + 类目装配表），regen 收敛为薄封装、CI yml 退化为单步骤调用——本地绿 = CI 绿，同一事实源 | 任何重生成/验证入口一律调 cicd verify；新类目 = 装配表加一行 |
| B5 | 收敛 yml 时把「环境准备」步骤一并删掉：决策 B 将 CI 收敛为「checkout + setup-python + 单步骤验证链」，误删 `pip install pytest`（初版 d5d6640 有）——本地开发机装过 pytest 全绿，CI 纯净 Python 3.12 `No module named pytest` 首跑即败 | **已修复（2026-08-03）**：ci.yml 恢复「安装依赖（仅 pytest）」步骤，注释标注原因；教训：**环境准备与验证链是两个关注点，收敛编排时环境步骤是前置依赖不是可删冗余**；本地绿 ≠ CI 绿的环境差异（装过的包）由 CI 首跑暴露，决策 B「本地=CI」同一事实源只覆盖命令形态，不覆盖依赖环境 | 任何 CI yml 改动后检查：runner 纯净环境（无 pytest 等）→ 环境准备步骤必须保留；改完 yml 先推一个提交看 CI 首跑而非只信本地 |

## 三、审计与交接类（五轮审计 + 三轮返工的教训）

| # | 问题 | 沉淀机制 | 后续参考 |
|---|---|---|---|
| C1 | 数字漂移：交付文档/报告旧数字残留（benefit 1.5% 为错误统计、feat 中位 195 vs 196、chunk 5948 vs 6022），审计反复抓 | 交付前用脚本实测重算数字；口径变化（全库 vs 模块内）必须标注；修复前后对比用 bak 备份实证 | 所有交付文档数字附「实测命令 + 口径说明」 |
| C2 | 交付失实（claims 与实际产物不符）导致五轮返工 | 审计链：交接 → 审计要求 → 交付审计 → 审计报告，独立复测原则；k3 审计要求模板 | 沿用「审计-返工文档协议」，后续模块 k3 审计直接复用 |
| C3 | 幻影数据（raw_example 84 个造假）跨文件串扰难发现 | t63 三闸口参数化固化（v2.2 决策 G，2026-08-03）：`vectorizer/prepare/` 包 `python3 -m vectorizer.prepare.census`（逐字普查）/ `firstlast`（自包含闸口）/ `phantom`（三分类取证，`--category` 语义表注册）；**五合一机检同模式参数化**（`vectorizer/prepare/scan.py`）+ **勘探校验同模式参数化**（`vectorizer/prepare/verify.py`，种族开工补强——专长 prepare_scan.py / verify_prepare.py 出现第二使用方时按禁 copy 原则抽公共骨架，同源对账 0 差异后专长侧冻结保留；verify 的 doc_roles/trap_kinds 词表注册于 categories.py `verify_prepare` 键） | 后续模块 Prepare Phase 直接复用三闸口 + scan + verify（新类目注册语义表即可） |
| C4 | 元数据 schema 只看少量样本定稿 → v0.3 返工 | 勘探数据驱动（字段标签/类型分布统计）+ **人工确认门**（k2.7） | 已写入开工前清单，装备/怪物模块照做 |
| C5 | 规划/审计中发现 bug 被顺手修掉中断主线 | 全局规则：只记录（KN 登记），修复另立任务 | 已固化进全局 CLAUDE.md |

## 四、源数据操作类

| # | 问题 | 沉淀机制 | 后续参考 |
|---|---|---|---|
| D1 | organized 源数据 CRLF 行尾，脚本不保留行尾 → diff 整文件污染 | 逐行保留原行尾（`re.split(r"(\r?\n)")` 模式） | 已写入项目 CLAUDE.md，后续脚本一律遵守 |
| D2 | 正则硬修不符合源数据真实形态（Uch→掉链子、`\r` 表格定位符、裸标题无前导星） | 「先理解后向量化」原则 + 判别器矩阵兜底 | 已写入 CLAUDE.md 约束与规范 |
| D3 | 正则字符串无效转义（`\*`、`\s`）SyntaxWarning，将来会变 SyntaxError | 已登记未修（ast 扫描时发现） | 新写正则一律用 raw string，存量待清 |

## 五、质量闸口类（本次新固化）

| # | 问题 | 沉淀机制 | 后续参考 |
|---|---|---|---|
| E1 | chunk 质量只有「OK 数合理」无硬性护栏 | `verify_data_invariants.py`：空 text ≤51 / >5000 字符 =0 / 总数下限 / feat 下限，**只收窄不放宽**（改动需在 KN 登记） | 新模块扩展不变量（法术可加 SR 真值、领域交叉类断言） |
| E2 | verify 分层不清：结构验证与数量护栏混在一起 | 职责分离：verify_feats 管判别器/召回/字段健康，不变量脚本管数量护栏 | 已写入 CLAUDE.md CI 小节 |
| E3 | 正则缺陷类（KN014/017/018/020）源扫描不可见，靠多轮审计才暴露 | 「产出后首轮即检三件套」：title 合法性扫描 + 字段值健康度 + 列表召回率 + 新正则全量触发计数 + 高危模式 probe（详见开工前清单 §四，导航挂 WORKFLOW「标准处理子流程」） | 新模块产出 chunk 后首轮必检，把发现时间从多轮审计压到首轮产出 |
| E4 | normalize 新增拆行规则改变了后续行形态：`_split_field_chain_star_shells` 把字段链拆成独立行后，豺狼人 `**价格：**11000gp **重量：-**` 整行首尾星壳（`**重量：-**` 值在星内致行尾闭星）——被 split 层既有「散文行星壳剥除」分支 1（ISR 残留语义）误当残留剥壳（KN102 同家族，race 实战） | 教训：**上游拆行规则的产物要过一遍下游剥壳/剥星守卫检查**——拆行可能制造「首尾星壳整行」新形态，命中既有剥壳语义；修复用 TDD 簇 I3 2 测试 + 守卫「剥壳后仍含 `**` 不剥」（ISR 合法输入剥壳后无星不受影响） | 后续任何拆行/拆链类修复：检查拆后产物是否 startswith+endswith `**` 整行；剥壳守卫统一「剥壳后无星才剥」口径 |
| E5 | **输入范围缺口**（2026-08-04，种族人工抽查暴露）：类目输入只取了 `organized/<类目>/` 子树，organized 根目录留档区同 CHM 目录页被漏掉——种族 9 页（KN107 P0：7 核心种族详情页+神裔+魔裔，标准特性/体貌/社会等板块全部无 chunk）、职业 3 页（KN108）、专长 2 页（KN109）；判别器 est/got 与召回基线都测不出「从未进入输入」的文件 | **数据范围硬要求：类目输入范围 = CHM 目录该类目子树对应全部文档 ∪ `pf_rules_md_organized/` 全量的并集，不得缺失 CHM 目录对应文档**。工具已固化：`audit_chm_scope_gap.py`（CHM TOC × md_mapping × organized 三方比对，URL 解码口径，输出 `docs/chm范围缺口排查_*.md`）；注意两个口径陷阱：①per_file 勘探清单 ≠ 真实输入（page_195 实证）；②根目录留档区「均为重复副本」假设要逐页验证（page_12~18 根目录即唯一副本） | **后续每个模块 Prepare Phase 第 0 步必跑 `audit_chm_scope_gap.py`**，缺口逐项定性（纳入/索引页豁免/重复副本豁免）后才允许开工；装备/怪物模块照做 |

## 六、横切观察与使用方式

- **机器可强制**（A1/A2/A3/B1/B3/B4/E1/E2/E5）：已用 CI 闸口、finalize 内建后处理（决策 A）、cicd verify 单命令（决策 B）、不变量断言、`audit_chm_scope_gap.py` 输入范围比对固化——后续模块自动继承。
- **人+流程**（B2/C1~C5/D1~D3）：靠审计链、t63 闸口、人工门、KN 登记——后续模块开工前读本清单 + 开工前清单，一次配齐。
- **使用方式**：新模块开工 → 读本清单 §一~§五 + `专长模块开工前_流程抽象与预防清单.md`；处理数据中途遇问题 → 按类别查对应条目；新增教训 → 追加到对应类别并 git 提交。
- **标准处理子流程导航**：操作类标准路径（格式杂乱聚合文档重建 SOP、校验问题处理、HTML 回退解析）统一挂在 `WORKFLOW.md`「标准处理子流程」小节（按需加载），不在此复制全文。
