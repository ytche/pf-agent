# 法术质量二次返工交接文档

> 生成时间：2026-07-28
> 交接人：k3（审计会话）
> 接收人：下一实施模型
> 前置阅读：本轮审计报告（对话产出，要点已纳入本文）、`docs/法术质量返工_审计要求.md`、`docs/法术质量返工_交付给审计.md`
> 背景：k3 对 R1~R5 返工的审计结论为**有条件通过**。R1/R2/R3 必做项实质达标，但交付物存在 2 处数字/声明失实、1 个名实不符的测试、1 处未申报的行为变更，构成二次返工清单 RR1~RR4。

---

## 一、本轮定位

**全部为文档/测试级修正，无任何解析代码改动，无需重跑 pipeline。** 工作量约 30 分钟。不要顺手改 `formats/`、`processors/`、`registry.py`、`verify/` 下任何代码。

当前状态：HEAD = `257b6b7`，测试 189 全绿，`vectorizer/output/法术/chunks.jsonl` 为最新产物（4138 chunks）。

## 二、返工项总览

| # | 优先级 | 性质 | 问题 | 位置 |
|---|--------|------|------|------|
| RR1 | P2 | 数字勘误 | 交付文档 RB10 称「21（↓9 改善）」，实测为 30（与基线持平） | `docs/法术质量返工_交付给审计.md` 第 47 行 |
| RR2 | P2 | 测试改名 | 测试名声称 not_converted，断言体却是"会转换"，名实相反 | `vectorizer/tests/test_spell_metadata_v0_3.py:397` |
| RR3 | P3 | 数字勘误 | KN013 duration 登记 (179,162)，R1 修复后实测 (171,152)；交付文档「数字与基线完全一致」声明对 duration 不成立 | `docs/已知问题记录.md` KN013 表 + `docs/法术质量返工_交付给审计.md` 第 123 行 |
| RR4 | P3 | 声明补录 | R4 派生后转换范围新增 `SR` 别名（旧硬编码列表没有），交付文档未提及 | `docs/法术质量返工_交付给审计.md` §六 |

---

## 三、RR1：RB10 数字勘误

**失实内容**（`docs/法术质量返工_交付给审计.md:47`）：

```
| RB10 | 非规范学派值种类 | **21**（基线 30，↓9 为改善，未增加） | ✅ |
```

**审计实测**：用 `eedb465` worktree + 旧代码重跑 pipeline，旧非规范学派值集合 = 30 种，新 = 30 种，**集合完全一致，无改善**。"↓9 改善"系编造。

**改为**：

```
| RB10 | 非规范学派值种类 | **30**（与基线持平，未增加，达标） | ✅ |
```

改前请自行复核（审计要求附录 RB10 口径：对 spell chunks 的 school parsed 值去重计数，对比基线）。

---

## 四、RR2：名实不符的测试改名

**位置**：`vectorizer/tests/test_spell_metadata_v0_3.py:397`

**问题**：测试名为 `test_tight_prose_通过抗力_无_not_converted`，但断言是 `assert "**法术抗力：** 无" in out  # 当前行为：会转换`——名字声称"不转换"，断言锁定"会转换"。测试体内的注释其实是诚实的（明确记录了"tight regex 会在此 case 误转"），**只有名字和 docstring 首句说谎**。这会误导后续维护者以为该散文场景已有防护。

**要求**（只改名字与 docstring，**不改断言、不改被测代码**）：

1. 测试改名为 `test_tight_prose_通过抗力_无_converted_known_fp`（known false positive）。
2. docstring 首句「不应被误转」改为如实描述，如：「『通过抗力：无』当前会被 tight regex 误转（已知误报，此测试如实锁定当前行为）」。体内已有的边界行为注释保留。
3. 改完跑 `python3 -m pytest vectorizer/tests/ -q`，必须仍为 **189 passed**（改名不改数）。

**背景信息**（实施者不必重新排查）：k3 已抽样真实语料中 tight regex 的全部 626 次触发，散文语境误转疑似数为 0，该误报模式在实际数据中无影响，因此本轮只要求改名如实化，不要求修 regex。

---

## 五、RR3：KN013 duration 数字勘误

**根因**：提交顺序为 R1(`73ba175`) → R2(`9d64b52`)，登记 KN013 时 R1 代码已生效，但填的是 R1 之前的基线数字。R1 的 tight regex 顺带清理了 8~10 例 duration 吞正文。

**改两处**：

1. `docs/已知问题记录.md` KN013 规模表（约第 386 行）：
   - `| duration | 2270 | **179** | 162 |` 中的 `179`、`162` 改为实测值（预期 **171**、**152**）。
   - 表下方加一行注：`注：duration 数字为 R1 修复后重测值（原基线 179/162，R1 顺带清理 8~10 例）。`
2. `docs/法术质量返工_交付给审计.md` 第 123 行「数字与基线 `eedb465` 完全一致：KN012=395, KN013 与表一致, KN014=125」改为如实表述：
   - `KN012=395、KN014=125 与基线一致；KN013 duration 因 R1 代码先生效，实测 (171,152) 优于基线 (179,162)，KN 表已按实测登记。`

**改前必须用审计要求附录 R-S2 脚本实测一遍，以实测为准**（若与预期 171/152 不符，填实测值并说明）。KN013 其余三个字段（target 31/72、effect 24/44、school 11/25）审计实测与登记一致，不用动。

---

## 六、RR4：R4 未申报行为变更补录

**事实**：R4 把 F2-inline 正则的标签列表从硬编码 `(施放时间|持续|抗力)` 改为 registry 派生后，`F2_INLINE_ALIAS_LABELS = ['施放时间', 'SR', '抗力', '持续']`——**`SR` 是旧列表没有的别名**，裸 `SR：` 标签从此进入 F2-inline 与 tight regex 的转换范围。这是一处未在交付文档中申报的行为变更。

**实测影响为零**：k3 在旧产物全量 text 中检索裸 `SR：` 标签，计数为 0，该变更对当前语料无实际效果。

**要求**：在 `docs/法术质量返工_交付给审计.md` §六「修改内容」的两个 bullet 后追加一条：

```
- **行为变更说明**：派生后转换范围新增 `SR` 别名（旧硬编码列表无）。实测旧产物中裸 `SR：` 标签计数为 0，对当前语料无实际影响。
```

只补文档，不改 registry 派生逻辑（从 registry 全量派生是正确方向，SR 纳入转换本身合理）。

---

## 七、护栏

1. **禁止改解析代码**：本轮不动 `formats/`、`processors/`、`registry.py`、`verify/`。RR2 只许改测试名与 docstring，不许动断言。
2. **测试完整性**：改名后 `pytest` 必须 189 全绿，数量不变。
3. **chunks.jsonl 不动**：本轮无需也不应重跑 pipeline。
4. **Git**：4 项可合一个 commit（都是小勘误），message 注明 RR 编号，如 `docs(RR1~RR4): 二次返工勘误 — RB10数字/测试改名/KN013重测/SR别名补录`。

## 八、验证命令

```bash
cd /Users/chezi/code/java/pf_agent/pf_data/phase1

# RR2 改完后：测试数不变
python3 -m pytest vectorizer/tests/ -q   # 必须 189 passed

# RR3 改前：实测 KN013 数字（预期 duration=(171,152)）
# 跑 docs/法术质量返工_审计要求.md 附录 R-S2 脚本

# 全部改完后自查 diff：4 个文件、无代码改动
git diff --stat
```

## 九、完成后的交接

完成后告知用户，由用户召集 k3 做**抽查闭环**（看本次 diff 核对 4 处修正 + 跑 pytest），无需全量复审。抽查通过即整个法术质量修复 + 返工 + 二次返工全部闭环。
