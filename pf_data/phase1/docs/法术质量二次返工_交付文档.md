# 法术质量二次返工 交付文档

> 交付时间：2026-07-29
> 实施者：k2.7
> 审计对象：k3（抽查闭环）
> 审计标准：`docs/法术质量二次返工_交接文档.md`（k3 审计会话产出）
> 前置：R1~R5 返工（commit `257b6b7`），k3 审计结论为**有条件通过**，二次返工清单 RR1~RR4

---

## 〇、总体结论

RR1~RR4 四项二次返工全部完成。**全部为文档/测试级修正，无解析代码改动，未重跑 pipeline。** pytest 189 全绿。

**1 个 commit，3 个文件，8 行增 5 行删。**

---

## 一、提交清单

```
bb72394 docs(RR1~RR4): 二次返工勘误 — RR1 RB10数字/RR2测试改名/RR3 KN013重测/RR4 SR别名补录
```

- 基线 `257b6b7` → HEAD `bb72394`，无夹带改动
- 未改动 `formats/`、`processors/`、`registry.py`、`verify/` 下任何代码

---

## 二、返工项明细

### RR1：RB10 数字勘误（P2）

**位置**：`docs/法术质量返工_交付给审计.md:47`

**修正前**：`**21**（基线 30，↓9 为改善，未增加）`

**修正后**：`**30**（与基线持平，未增加，达标）`

**实测依据**：用 `eedb465` worktree + 旧代码重跑 pipeline，旧非规范学派值集合 = 30 种，新 = 30 种，集合完全一致。

---

### RR2：名实不符的测试改名（P2）

**位置**：`vectorizer/tests/test_spell_metadata_v0_3.py:397`

**修正前**：
- 测试名：`test_tight_prose_通过抗力_无_not_converted`
- docstring 首句：`「通过抗力：无」不应被误转`

**修正后**：
- 测试名：`test_tight_prose_通过抗力_无_converted_known_fp`（known false positive）
- docstring 首句：`「通过抗力：无」当前会被 tight regex 误转（已知误报，此测试如实锁定当前行为）`

**约束遵守**：断言未改、被测代码未改、体内边界行为注释保留。

**背景**：k3 已抽样 tight regex 626 次触发，散文语境误转疑似数为 0，该误报模式在实际数据中无影响，因此只要求改名如实化。

---

### RR3：KN013 duration 数字勘误（P3）

**根因**：提交顺序 R1(`73ba175`) → R2(`9d64b52`)，登记 KN013 时 R1 代码已生效，但填的是 R1 之前的基线数字。R1 的 tight regex 顺带清理了 8~10 例 duration 吞正文。

**改两处**：

1. `docs/已知问题记录.md` KN013 规模表：
   - `duration | 2270 | 179 | 162` → `duration | 2270 | **171** | 152`
   - 表下方加注：`注：duration 数字为 R1 修复后重测值（原基线 179/162，R1 顺带清理 8~10 例）。`

2. `docs/法术质量返工_交付给审计.md` 第 123 行：
   - 修正前：`数字与基线 eedb465 完全一致：KN012=395, KN013 与表一致, KN014=125`
   - 修正后：`KN012=395、KN014=125 与基线一致；KN013 duration 因 R1 代码先生效，实测 (171,152) 优于基线 (179,162)，KN 表已按实测登记。`

**实测复测**（R-S2 脚本）：
```
KN013 (含句号/超长): {'duration': (171, 152), 'target': (31, 72), 'effect': (24, 44), 'school': (11, 25)}
```
target/effect/school 与登记一致未动。

---

### RR4：R4 未申报行为变更补录（P3）

**位置**：`docs/法术质量返工_交付给审计.md` §六「修改内容」

**追加内容**：
> - **行为变更说明**：派生后转换范围新增 `SR` 别名（旧硬编码列表无）。实测旧产物中裸 `SR：` 标签计数为 0，对当前语料无实际影响。

**事实**：R4 把 F2-inline 正则标签列表从硬编码改为 registry 派生后，`F2_INLINE_ALIAS_LABELS = ['施放时间', 'SR', '抗力', '持续']`——`SR` 是旧列表没有的别名。k3 实测旧产物中裸 `SR：` 计数为 0，无实际影响。从 registry 全量派生是正确方向，SR 纳入转换本身合理，仅补文档。

---

## 三、验证结果

### pytest

```bash
python3 -m pytest vectorizer/tests/ -q
# 189 passed in 0.86s
```

### git diff

```
docs/已知问题记录.md                              | 4 +++-
docs/法术质量返工_交付给审计.md                    | 5 +++--
vectorizer/tests/test_spell_metadata_v0_3.py       | 4 ++--
3 files changed, 8 insertions(+), 5 deletions(-)
```

无 `formats/`、`processors/`、`registry.py`、`verify/` 改动。

---

## 四、护栏遵守

| 护栏 | 状态 |
|------|:--:|
| 禁止改解析代码（`formats/`、`processors/`、`registry.py`、`verify/`） | ✅ |
| RR2 只改测试名与 docstring，不断言 | ✅ |
| pytest 189 全绿，数量不变 | ✅ |
| chunks.jsonl 未重跑 | ✅ |

---

## 五、交付物清单

| 文件 | 改动类型 | 关联 RR |
|------|----------|:--:|
| `docs/法术质量返工_交付给审计.md` | 勘误 3 处 | RR1, RR3, RR4 |
| `docs/已知问题记录.md` | 数字修正 + 注释 | RR3 |
| `vectorizer/tests/test_spell_metadata_v0_3.py` | 测试改名 | RR2 |

---

以上。k3 可按 `docs/法术质量二次返工_交接文档.md` §九做抽查闭环：看本次 diff 核对 4 处修正 + 跑 pytest。抽查通过即**整个法术质量修复 → 返工 → 二次返工全部闭环**。
