# 专长 R1 三聚合文件重建 — 交付审计（2026-08-02）

> 交付方：Claude（后台任务）｜交付对象：审计方（独立 Agent）
> 审计方式：**直接运行交付方同一脚本独立复算**，禁止另写窄口径脚本"证明通过"（补丁 #3）
> 归属提交：`8b283d1`（三聚合重建）+ `808245d`（工作流 SOP），经 PR #6 → PR #5 合并进 `feat/feat-module`（HEAD `e7d93a8`）
> 验收口径：`docs/专长/专长审计要求与豁免程序.md`（流程补丁包 #3~#8）

---

## 0. 交付物总览（审计范围）

| 类别 | 文件 | 变更 |
|---|---|---|
| 源数据重建 | `pf_rules_md_organized/专长/初探探索者协会_专长.md` | 15 条目（← page_547） |
| 源数据重建 | `pf_rules_md_organized/专长/药剂与毒药P&P_专长.md` | 14 条目（← page_1258 + page_1263） |
| 源数据重建 | `pf_rules_md_organized/专长/动物档案AArch_动物专长.md` | 13 条目（← page_437） |
| 重建脚本 | `docs/专长/build_feat_md_aggregate_rebuild.py` | 新增，可复算 |
| 豁免登记 | `docs/state/feat_exemptions_20260801.md` | 豁免 6→3，k2.7 已解决 |
| 方法沉淀 | `docs/格式杂乱聚合文档标准处理工作流.md` | 新增 SOP |
| 交付记录 | `docs/专长/三聚合文件HTML重建_交付_20260801.md` | 上轮交付文档 |

## 1. 一句话交付摘要

三个「整段粘连、无条目边界」的聚合源文件（判别器 est 正确但切分失败 got=0/1），按原始 CHM HTML 重建为规范条目结构后 **est↔got 精确匹配（15/15、14/14、13/13）**，原 3 条整文件豁免撤销（6→3），第二轮审计 k2.7 人工门**实证闭环**（无需人工批准豁免）。

## 2. 验收断言与复算命令（补丁 #3/#5）

**每个 ✅ 必须附复算命令跑出，禁止手填。**

| # | 断言 | 复算命令 | 交付方实测 |
|---|---|---|---|
| A1 | 三文件条目数与 HTML 专长数一致（15/14/13） | 运行重建脚本产 draft 后人工核对条目数 | 42 条，est=产出 15/15、14/14、13/13（**got 指产出 chunk 数，非矩阵 `fc_deviation_matrix.json` 的 got 列**——矩阵 got 是判别器自身口径 16/14/12，verify 回归对账的是「产出 vs est」，不拿矩阵 got 列对账） |
| A2 | 判别器回归：静默丢失 0 | `PYTHONPATH=. python3 vectorizer/verify/verify_feats.py` | 非豁免 est=3623 got=3762，**静默丢失 0** |
| A3 | 全链重产出 EXIT=0，chunk 总数可解释 | `bash regen_feat_chunks.sh`（单入口） | **3747 chunks**（原 3706，+41 = 重建 42 条 − 原粘连误产） |
| A4 | 六形态分布与落地表一致 | verify 输出 format_cluster | standard 1769 / elided 758 / whitespace 164 / lb_marker 954 / punct 10 / img_marker 92 |
| A5 | pytest 全绿 | `python3 -m pytest vectorizer/tests/test_feat_format.py vectorizer/tests/test_feat_processor.py -q` | 通过 |
| A6 | verify 五检查全绿（含 R5 probe） | 同上 verify 命令 | elided 758 个，正文句段碎片 **0** |
| A7 | 召回 838/1072=78.2% | verify 输出 | 一致；未命中 234 条为 K5 核心书源缺失遗留，不阻断 |
| A8 | 豁免文档 6→3，判别器回归按全量 189 rel 对账 | 读 `docs/state/feat_exemptions_20260801.md` + verify | 保留 page_668/page_694/专长30（理由充分或主版本已产出） |
| A9 | 来源解析保留 | `grep -c "> 来源："` 三文件 | 42 条均有 `> 来源：`（通道 1 逐字保留），source 注释前缀保留 |

## 3. 审计要点（重点核对项）

1. **字段值折叠正确性**（A1 子项）：重建脚本把多行字段值折叠为单行——若漏折叠，续行（中文开头+英文括号）会被 `formats/feat.py` 的 M6/M9 裸中英行逻辑误判成幽灵条目标题（实测动物档案「魔宠法术·好处」多行值曾拆出第 14 条）。审计反向验证：draft 中该字段应为单行、无残留。
2. **非条目区无残留**（A1 子项）：page_1258 变体（曼特蛙/沼泽毒杀者/毒刃/叉舌门徒）、血脉（蝎子血脉/剧毒遗产）、page_1263 法术（适饮法术）不应混入任何条目字段值。
3. **条目名清单人工核对**：`build_feat_md_aggregate_rebuild.py` 的 `ENTRIES` 42 个 (zh, en) 为人工阅读确认——审计抽查若干条目与 HTML 原文对齐。
4. **标题形态**：`**中文（English）**` + 标题尾双空格，字段标签 `**字段：**值` 规范，描述 `*...*` EM 包裹。
5. **豁免撤销与 k2.7 语义**：3 条豁免撤销的判据是"重建后 est↔got 匹配"（实证产出），而非"人工批准豁免"——符合补丁 #4「执行方无单方豁免权」语义（豁免不再需要，回到正常断言路径）。

## 4. 红线（不可触碰）

| 红线 | 依据 |
|---|---|
| `fc_deviation_matrix.json` 只读 | 判别器回归基准（189 rel，est=3635/got=4264），改动必须回归此矩阵 |
| `术语提取报告_分类版.md` 只读 | 术语表基线 |
| spell/profession 模块不重构 | 已完成模块冻结（KN011~018 已闭环） |
| 禁止在 pipeline 硬编码 `if 书名 == "X"` | 全局规范；重建走数据层面，不动 `formats/*.py` 通用逻辑 |

## 5. 已知遗留（审计时一并核对，不阻断）

- **K5**：核心书源缺失致召回 234 未命中（UC101/CRB79/ACG33/ARG18/UM3）——一期上线前唯一建议修项（法术模块 KN019 同类）。
- **K8**：组织文件超产伪条目 + verify 无超产上限（结构性缺陷，质量迭代加超产上限）。
- **K9**：R5 probe 对表格行/节标题类伪条目盲区。
- **K10**：AG 边栏条目未产出（判别器亦未计，亚产能既有差距）。
- **K1~K7**：见 `产出即检_首轮_20260801.md`。

## 6. 审计程序要求

1. **同口径复算**：直接运行 verify/regen/pytest 同一脚本；禁止为"通过"另写窄口径脚本（补丁 #3 §2）。
2. **逐项附命令**：每个 ✅ 附复算命令与实测输出（补丁 #5）。
3. **条数 vs 计数一致**：本文档 §0 声称的条数/文件数与仓库实际逐一核对（补丁 #6）。
4. **旧数字裸 grep**：`grep -rn "3706\|4214\|1067\|84.6%" docs/` 确认无交付文档残留旧数字（补丁 #8）。
5. **发现问题**：逐条登记 → 若属返工项按 `专长TDD_返工交接_第一轮_20260801.md` 结构出具返工清单；若属登记项补登 KN。
6. **豁免不得单方放行**：新豁免逐项书面申请（rel/est/got/理由带实测证据），落 `docs/state/`。

## 附 · 复算命令速查

```bash
cd /Users/chezi/code/java/pf_agent/pf_data/phase1

# 全链重产出 + pytest + 验收（单入口，交付方以此为准）
bash regen_feat_chunks.sh

# 验收独立复算（与 regen Step 4 同一脚本）
PYTHONPATH=. python3 vectorizer/verify/verify_feats.py

# pytest 独立复算
python3 -m pytest vectorizer/tests/test_feat_format.py vectorizer/tests/test_feat_processor.py -q

# 重建脚本复算（产 draft 到 docs/专长/_rebuild_draft/，人工核对后对比正式文件）
python3 "docs/专长/build_feat_md_aggregate_rebuild.py"

# 判别器单文件对账
python3 vectorizer/exploration/专长/fc_match.py --file 专长/初探探索者协会_专长.md
```
