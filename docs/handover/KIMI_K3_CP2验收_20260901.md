# KIMI_K3 验收文档 · CP2 KN222 修复

> 批次：CP2 · 阶段一
> 日期：2026-09-01
> 验收人：k3
> 关联交付：《KIMI_K3_CP2批次交付_20260901.md》（commit 6816188）
> 关联方案：《职业法术模块重整方案_20260826.md》§4 CP2、V1-2
> 结论：**验收通过**

---

## 一、验收结论

CP2（KN222 修复）**验收通过**，允许进入 CP3。所有关键数据均经 k3 亲跑复算，与交付文档声称一致；修复质量好于预期（连带找回了汇总文件空列表头形态与 page_796 公式续行吞正文两个计划外缺陷）。

## 二、k3 独立验证记录（全部亲跑，非转述）

| 验证项 | 方法 | 交付声称 | k3 实测 | 结果 |
|---|---|---|---|---|
| 改动面 | `git diff 8834e0e..HEAD` | 仅 profession 两文件 + 新测试 | formats +51 / processors +18 / test +253，零共享层 | ✓ |
| 16 个 TDD 测试 | 亲跑 pytest | 16 passed | 16 passed (0.90s) | ✓ |
| 新基线 | 亲跑新 pipeline 全量 | 13922（+27） | **13922**（before 13895，+27） | ✓ |
| 字符零丢失 | 字符多重集对比 before/after | 仅 before 字符 = 0，净找回 53480 | **仅 before 字符 = 0** | ✓ |
| 简写表头页找回 | k3 独立扫描源文件再核对产物 | 54/54（46 源页 + 8 汇总） | k3 独立扫描得 54 页，找回 **54/54** | ✓ |
| page_754 冬女巫 | 产物抽查 | 主块含先决条件+简写等级表 | 主块在，先决条件在，等级表 **10 个数据行完整** | ✓ |
| 块级变化性质 | 仅老 328 / 仅新 355 采样 | 全为标题清理类改进 | 数量一致；采样确认「过渡句污染标题 → 干净能力标题」 | ✓ |
| KN222 登记更新 | 读已知问题记录.md | 状态已更新 | 已更新（标注待 k3 验收闭环） | ✓ |
| CP1 更正要求落实 | 读 3c8daaf | — | 文档移根部、基线口径修正、terms/aliases 口径补全、commit 标签改正 | ✓ |

### 测试质量评估

16 个用例非凑数：覆盖简写表头 6 形态（含空列 `||`）、全称表头回归、魅影表/正文不误伤、嵌套粗体、散星号、字段标签、【】变体单行、page_796 公式续行、page_754 集成找回，每个用例带根因注释。「修复前红」为结构性可信（断言对象是修复前不存在的行为），未逐条 revert 复验。

### 代码审查要点

- `_looks_like_class_table`（processors/profession.py:1878）：简写锚定 `等级+BAB+强韧+反射+意志` 五列连续匹配，去全半角空格；魅影表（HD/豁免/技能列）天然不匹配，不误伤。改动最小、注释完整。
- `promote_inline_ability_headings`（formats/profession.py:452）：句末标点切分 + 三重守卫（cand 自身信号 / lead 不含【】 / 无标点不剥离）+ pm_rejected 窄分支退化 title-only。v1 全局 title-only 改法曾致 -4439 严重回归，执行方自行回退并收窄——**这个迭代纪律值得肯定**。

## 三、口径说明（k3 认可）

1. **54 页 vs KN222 原文 40 页**：k3 独立扫描源文件得 54 个简写表头页（含 8 个汇总文件），与交付口径一致；KN222 登记时的「40 页」是 08-01 时的统计口径/源文件状态，以本次实测为准。KN223 已证明 08-09 后源数据环境有漂移，口径演进属正常。
2. **feat/race 回归**：本批零共享层改动（git 实证），feat/race 在结构上不可能受影响，豁免三类目回归跑动。CP3 接 cicd 后由验证链常态覆盖。
3. **净增 +27 与找回字符 53480**：字符零丢失（before ⊆ after）前提下净增为纯找回内容，无膨胀风险。

## 四、遗留事项（不阻塞，转入 CP3/CP4）

1. KN222 检索闭环依赖 CP4 向量库重建（`vector_store_classes.json`），当前 Java 侧仍读 07-24 老产物——**这是方案既定节奏，不是缺陷**。
2. page_754 主块 component_type 归 `class_feature`（既有归类规则，测试已注释说明），内容找回优先，归类优化如有需要另行立项。
3. 部分页先决条件以独立块标题存在（交付 §七.3），内容完整，属既有归类。

## 五、下一步（CP3 要点提醒）

按方案 §4 CP3 执行，k3 验收时将重点亲验：

1. `cicd/verify.py` profession 类目四步链全绿（pytest 收集 → 真重跑 → verify → 不变量闸口）；
2. 13 个根目录独立 `verify_*.py` 收敛去向（pytest/verify 体系或归档，k3 定夺）；
3. **invariants 闸口阈值必须按 13922 新基线实测设定并注释口径**（参照 race 显式传参模式），禁拍脑袋；
4. 540 源文件 diagnostics 门禁通过记录；
5. 真重跑产物与 CP2 after（13922）一致——CP3 不允许再引入行为变化。

CP3 交付文档沿用四要素 + 可复现数字硬要求。
