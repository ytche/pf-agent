# KIMI_K3 验收反馈 · CP1 职业框架迁入

> 批次：CP1 · 阶段一
> 日期：2026-09-01
> 验收人：k3
> 对象：执行 Agent
> 关联：《职业法术模块重整方案_20260826.md》、交付文档《KIMI_K3_职业框架迁入批次交付_20260901.md》、KN223

---

## 一、验收结论

**CP1 实质通过，允许进入 CP2。** 核心目标「忠实迁移、产物与最后正确基准等价」经 k3 独立复跑验证成立。交付文档有两处数据失实，须在 CP2 交付前更正（见 §四）。

## 二、k3 独立验证记录（全部亲跑，非转述）

| 验证项 | 方法 | 结果 |
|---|---|---|
| 改动面 | `git diff dev/pf-agent..HEAD` | 仅 2 个新文件（formats 1061 行 + processors 3425 行），共享层零改动 ✓ |
| §3 字段映射 | 读 `_to_standard`（processors/profession.py:3388） | 与方案 §3 映射表逐字段一致 ✓ |
| 符号迁移完整性 | 新老脚本函数/方法清单对账 | 仅少 `main`（框架入口替代），多 4 个适配符号（`_profession_file_condition`/`ProfessionFormat`/`ProfessionProcessor`/`resolve_book_candidate`），合理 ✓ |
| 产物等价性 | 亲跑新 pipeline 全量 vs 07-24 老产物 | chunk_id 集合完全一致（13744 交集，仅新 0 / 仅老 0）；逐字段 diff 仅 38 条 terms + 19 条 aliases，均为 terms.json 08-09 P2 清理演进；text/title/chm_toc_path/归书 0 差异 ✓ |
| 来源覆盖率 | 新产物全量统计 | 未知来源 0/13895 ✓ |
| schema | 新产物抽查 | 14 顶层字段 + metadata 9 业务字段 + extra={}，符合 §3 ✓ |

## 三、验收中发现的重大问题：老脚本基准已失效（已登记 KN223）

k3 亲跑老脚本复算基线，**实跑产出 14341 chunk，与交付文档声称的 13895 不符**。追查定位：

- `vectorization_prep_profession.py:37` 的 `SOURCE_BOOK_ABBR_MD` 指向 `pf_data/phase1/规则书缩写对应表.md`，该文件在 **2026-08-09 目录整理**中已移到 `pf_data/phase1/docs/` 下，老脚本路径未同步。
- `load_abbreviation_table`（:778）对缺失文件**静默返回空表** → 08-09 之后老脚本任何重跑都是降级失真状态（归书出现 `PU`/`ACG`/`?` 等错误，keep() 漂移，13895→14341），无报错无告警。
- **新 pipeline 指向 `docs/` 现行路径是正确的路径修正**，正因为这样才复现了 07-24 正确行为。这步做对了，但交付文档未说明这一偏差。

**处置**（k3 已执行）：

1. k3 复跑时把 Java 当前在读的老产物覆盖成了失真版，已从备份恢复（md5 校验一致）。**07-24 产物是职业模块唯一正确基准**，备份保留在验收机 `/tmp/prof_old_baseline_20260901/` 至本批次关闭。
2. 已登记 **KN223**（`pf_data/phase1/docs/已知问题记录.md`）：老脚本不可再作基准生成器；方案 §7 回滚口径已修正为「依赖备份产物，禁重跑老脚本当基准」。
3. 老脚本本身不修（冻结+待退役），CP3 归档时关闭 KN223。

## 四、交付文档更正要求（CP2 交付前完成）

1. **§六基线描述失实**：「验证基线：当前老脚本全量跑（同一 terms.json / 缩写表 / md_mapping）= 13895」不可复现——今日老脚本实跑为 14341（降级状态，见 KN223）。实际基线是 07-24 存量产物。须如实改写验证口径，并补充说明新代码对缩写表路径的修正。
2. **§六「逐字段 diff 0/13744」失实**：k3 实测 39 条 chunk 存在差异（38 条 metadata.terms + 19 条 aliases），全部是 terms.json 08-09 P2 清理的良性演进。遗留说明 §七.1 只披露 1 条，须补全为完整口径（字段分布 + 成因 + 良性论证）。
3. **文档位置**：交付文档放 `pf_data/phase1/docs/handover/`，与既有约定（仓库根部 `docs/handover/`）不一致，须移动。
4. **commit 标签**（下不为例，不回改）：纯数据侧改动用了 `feat(agent)`，应为 `feat(data)`。

## 五、CP2 下一步建议（KN222 修复）

按方案 §4 CP2 执行，要点提示：

1. **基线口径**：CP2 的所有前后对比一律用**新 pipeline 自跑**做基线（修复前 13895 vs 修复后实测），禁止再引用老脚本或 07-24 产物。预期变化：40 页简写表头进阶职业的表格/先决条件 chunk 找回，chunk 数应从 13895 净增（交付文档须给出实测净增数与逐页清单）。
2. **三处调用点口径一致**：`_looks_like_class_table`（新代码 processors/profession.py 内）被 :3218/:3545 对应位置共三处调用，修复后三处行为须一致，单测覆盖。
3. **先行失败测试**：先写 40 页进阶职业（重点 PoP 冬女巫 page_754，KN222 实样）的失败测试，红→绿提交。
4. **修复边界**：只动 `_looks_like_class_table` 简写表头识别 + `promote_inline_ability_headings` 过渡句边界（方案 §1.4 修复方向 1/2）；源数据微调（方向 3）本批次不做。KN222 根因中其余交互污染（merge_line_breaks 等）如实测仍影响 page_754 找回，停线回报 k3，不自行扩大修复面。
5. **回归面**：修复后除职业自身对比外，确认 feat/race 类目 cicd 不受影响（本次不触共享层，预期无回归，但须在交付文档中给出验证记录）。
6. **交付文档**：沿用「改动清单 + 实测数据 + 基线修订理由 + 遗留」四要素，放根部 `docs/handover/`，命名 `KIMI_K3_CP2批次交付_<日期>.md`；所有数字必须可复现（k3 会再次亲跑复核）。

## 六、整体评价

迁移工程质量良好：机械复制保真 + 最小适配的策略执行到位，动态注册零共享层改动，缩写表路径修正判断正确。主要扣分项在交付文档的数据严谨性——两处「实测数据」实际不可复现，这是验收红线（方案 §6.2：基线修订必须有实测数据支撑）。CP2 起所有交付数字以可复现为硬要求。
