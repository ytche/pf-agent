# KIMI_K3_SP6 验收文档（开源化工程）

- 日期：2026-09-09
- 验收人：kimi-k3（全部亲跑亲验，不信交付自述）
- 交付文档：`docs/handover/KIMI_K3_SP6批次交付_20260909.md`
- 方案：`pf_agent/agent/docs/SP6_开源化工程方案_20260908.md`
- 分支：`feat/sp6-open-source`（T1~T6 共 6 个独立 commit，`965a9be`~`6d76ac0`，未合并）

## 结论

**附条件通过**：V1~V5、V7 全部亲验通过；V6 README 走查发现 **1 项 P2 必修**（事实性错误，见下），修复后 k3 复核两处改动即可收口，无需重跑全套验收。

## P2 必修（仅 1 项）

**README「27 个职业」与数据事实不符（2 处）**。

- `README.md:41`「并内置 **27 个职业的环级法术清单**」、`README.md:48`「**法术清单直查工具**：27 职业环级清单」。
- 亲验事实：`pf_data/phase1/vectorizer/output/法术/spell_lists.json` 的 `classes` 字典实际 **26 个职业键**（27 是 CLASS_LISTS 注册表的源列表页路径数——召唤师/召唤师(掉链子) 同源页等原因页数≠职业数）；应用启动日志亦为「26 职业、10068 条法术」；SP3 批次全程口径均为 26。
- 修法：两处 `27` 改 `26`（可顺带注明「源自 27 个官方职业法术列表页」）。README 是公开库门面，数据声明必须与库内数据一致。

## 逐项验收记录（k3 亲验）

| # | 项 | 结果 | 亲验证据 |
|---|---|---|---|
| V1 | 文件齐备 | ✅ | 13 类文件逐一 `[ -f ]` 核对全部就位：LICENSE/LICENSE_EN/NOTICE.md/README/CONTRIBUTING/.env.example/docs/DEPLOYMENT.md/Dockerfile/.dockerignore/docker-compose.yml/agent-ci.yml/docker.yml/PR 模板；issue 模板 3 类（bug/feature/data_correction） |
| V2 | 合规与清洁 | ✅ | k3 亲跑 secrets 扫描（`sk-`{20,}/`ghp_`/`AIza`/`xox`/私钥头，全量 tracked 文件）：**零真实命中**；排除清单核对：`kimi-export-session_*`/`.claude/`/`.pytest_cache/`/`.env` 在 tracked 文件中命中 0；NOTICE.md §2 含 Paizo CUP 英文声明原文段（亲读） |
| V3 | 工程零回归 | ✅ | `git diff dev/pf-agent..feat/sp6-open-source -- src/main/java` **空**（零逻辑 diff）；k3 本地亲跑 `mvn test`：**247, Failures 0, Errors 0, BUILD SUCCESS** |
| V4 | Docker 实跑 | ⏸ 留验（边界属实） | `docker compose config --quiet` 通过；亲测 `curl registry-1.docker.io` 返回 000（连接失败），ghcr.io 返回 401（可达需认证）——交付文档「本机 Docker Hub 不可达」披露**属实**，本地 compose 实跑确被网络阻断，留翻转后实跑可接受 |
| V5 | CI/发布 | ✅ | `gh run list --repo ytche/pf-agent`：agent-ci/docker/数据闸口三 workflow 全 success；docker run 日志亲验 `writing image sha256:6322dc85…` + `naming to ghcr.io/ytche/pf-agent:latest done`；亲跑 `docker pull ghcr.io/ytche/pf-agent:latest` 复现 403 Forbidden（private 阶段预期边界，与披露一致）；仓库 `visibility: PRIVATE` 确认 |
| V6 | README 走查 | ❌ P2×1 | 以新用户视角通读 README/DEPLOYMENT/.env.example/compose/Dockerfile 全文。除 P2 外均合格：两种快速开始路径完整、配置表覆盖 yml 键、三坑齐全且与 SP2/SP5 验收口径一致、`/api/health` 端点亲证存在（ChatController.java:72）、Dockerfile jar 名与 pom（pf-agent-0.1.0-SNAPSHOT）一致 |
| V7 | 红线 | ✅ | 每 T 独立 commit（6 个，log 亲验）；未合并；库保持 private，翻转动作未执行；交付文档 §6 披露 9 项偏差/遗留均如实且均有核实 |

## 交付方披露的偏差/遗留复核（§6 九项）

逐项核对，**全部属实、处置合理**：

1. JRE 17 而非方案草案 21 —— 与 pom `java.version=17` 对齐，合理，Dockerfile 注释已留痕。
2. Java workflow 命名 `agent-ci.yml`（`ci.yml` 被数据闸口占用）—— 两 workflow paths 各自收窄，实测新库三 workflow 并存无冲突。
3. 数据侧 ci.yml 随公开库保留 —— 新库实测 5 job 全绿（run 34275241393），源数据在库内可现场复现；保留与否建议用户拍板时确认（默认建议保留，理由交付文档已述）。
4. `docker pull` 403 边界 —— k3 亲复现，翻转 public 后匿名补验。
5. V4 本地 compose 未实跑 —— 网络阻断属实，留验合理。
6. Release 预构建向量库包未上传 —— 属 public 后动作，README 已按可分离叙述，翻转后补传即可。
7. 开发流同步约定待建 —— 真实遗留，建议下一会话与 SP 系列收尾一并约定（旧库发布分支 squash 同步新库 main）。
8. CI action deprecation 警告 —— 非阻塞，事实。
9. README badge 指向新库、private 阶段占位 —— 事实。

## 修复与收口路径

1. 执行 Agent 修 P2（README 两处 27→26，独立 commit）。
2. k3 复核 diff（仅 README 两处），无需重跑 mvn test/CI。
3. 用户拍板后合并 `feat/sp6-open-source` → `dev/pf-agent` 并推送。
4. 后续 public 翻转 checklist（用户本人执行）：`gh repo edit ytche/pf-agent --visibility public` → 匿名 `docker pull` 补验 → Docker 实跑（V4 补验）→ 上传 Release 预构建数据包 → badge 点亮确认。

## 修复轮复核（2026-09-09，收口）

执行 Agent 修复 commit `c3e0a9e`，k3 亲验 diff：

- 仅 `README.md` 两处改动：`27 个职业` → `26 个职业`，且第 41 行顺带补注「源自 27 个官方职业法术列表页」，与数据事实（`classes` 26 键 / 27 源列表页）完全一致。
- 无其他任何文件改动；独立 commit，符合红线。

**SP6 验收正式通过，批次收口。** 遗留动作（均不阻塞）：合并推送后，新库 `ytche/pf-agent` 的 main 需同步本修复（发布快照取自修复前）；public 翻转 checklist 见上第 4 条，由用户本人执行。
