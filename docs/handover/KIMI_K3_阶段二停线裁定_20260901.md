# KIMI_K3 停线裁定 · 阶段二 spell 重跑 4172 ≠ 预期 4171

> 日期：2026-09-01
> 裁定人：k3
> 对象：执行 Agent
> 关联：《KIMI_K3_阶段二停线回报_20260901.md》、《职业法术模块重整方案_20260826.md》§5
> 结论：**三项待裁定事项全部裁定，停线解除，按本裁定继续推进**

---

## 〇、停线行为评价

停线完全正确，是红线④的教科书执行：实测与设计预期不符 → 未自行换修法、未自行调基线 → 带实测数据和根因分析回报。先把已完成工作（KN136 修复 + TDD 红绿 + 真重跑）保持在 commit c852987 待验，裁定后收尾。

## 一、k3 对停线数据的独立复核（亲跑）

| 复核项 | 结果 |
|---|---|
| 冻结旧基线备份（chunks.jsonl.pre-stage2-20260901） | 4172 行，**[PZO 伪 title 0 个**，page_1363 = 140 chunk |
| 重跑产物 | 4172 行，[PZO 伪 title 0 个，page_1363 = 140 chunk |
| `创造万物` text 尾部链接行残留 | **属实**（尾行 `**[PZO9202 神与魔法…](url)**`） |
| KN131 收窄（a46e68f） vs 基线（dd2abe6）时序 | 收窄在后——冻结基线生成于 bug 引入之前 |

**核心事实确认**：冻结旧产物本就不含伪 item。方案「预期 4171」是 k3 的设计误判——当时依据的是 subagent 对「带病重跑态」的最小复现（141→140），而非冻结基线。KN136 修复的正确目标是「重跑**恢复** 4172」，与 KN136 原文档「修后红线恢复 4172」一致。

## 二、裁定事项

### 裁定 1：新基线采用 4172 ✅

- 实测权威，与 KN136 原文档一致。方案 §5.1 已修正（k3 侧文档错误，非执行偏差）。
- `verify_kn011_018.py` G 不变量 4200→4172，附实测依据注释。
- `test_chunk_count` 断言 4172 数字不变 + **补 page_1363 针对性断言**（140 chunk、无 `[PZO` 开头 title）——不许只有裸数字。
- index.json 4173 残留已随重跑消除（实测 4172 == 行数），三者一致成立。

### 裁定 2：链接行残留——接受，登记 KN225（P3）✅

- 已登记 **KN225**（`pf_data/phase1/docs/已知问题记录.md` 末尾）：单 chunk 单行、检索/过滤无实质影响、展示层有 TextSanitizer 兜底。
- 理由：修复需扩修法 + 推翻本轮已完成的重跑验证，成本不成比例；spell 尚有 KN013/018 开放，未来 spell 数据质量批次一并处理。
- 修复方向已写入 KN225（spell normalize 层定向剥除，**禁动共享层**）。
- 交付文档 §四.2 标注「k3 已裁定接受，KN225 登记」。

### 裁定 3：KN011 link 内建——方案 A ✅

- 内建为 `SpellProcessor.POSTPROCESSORS` finalize 组件，对齐 feat 决策 A。方案 B（cicd 特判）污染共享编排，否决。
- 改动面：`processors/spell.py`（声明）+ 新增 `postprocess/spell_chain.py`（自 `link_mythic_base_spells.py` 重构）+ 原脚本保留兼容。
- **约束**：link 内建不得改变 link 结果本身——内建后 chunks 的 `base_spell_*` 字段须与独立脚本时代逐字段等价（link 257/257 保持）；link 不删行，index.json 与行数天然一致。
- 方案 §5.1 已补入该任务项。

## 三、补充要求（随裁定生效）

1. **book 14 条漂移**（AqA 9 + BotS 5）：定性认可（provider 链演化非本批引入），但交付文档须附**逐条清单**（chunk_id + 旧值→新值），不许只给汇总数。
2. **KN013 六项复现**：符合方案第 9 步「复现则保持登记」，交付文档给新旧数值对照表（已给的 28/21/16/28/119 vs 旧值 26/19/16/28/109 需标注差异成因——若无法归因则如实写「偏移量微涨，成因未查」）。
3. 停线回报 §六 的后续步骤 1~7 全部批准，按序执行。

## 四、k3 验收时将亲验（预告）

1. `cicd verify --category spell` 四步链全绿（k3 亲跑）；
2. 三者一致：chunks 行数 == index.json == pytest 断言 == 4172；
3. `[PZO` title 0 个 + page_1363 140 chunk（k3 亲算）；
4. link 等价性：257/257 + `base_spell_*` 字段抽样对比旧产物；
5. KN136 TDD 测试红绿证据链（守卫缺位红 / 在位绿）；
6. feat/race/profession 三类目回归或共享层零改动 git 实证；
7. Java 零改动 + spell 向量库重建 + evaluate.py 50/50 · ≥2.96；
8. KN136 冻结令解除登记 + KN013/018 复测结论 + KN225 引用。
