# KIMI_K3 停线回报 · 阶段二 spell 重跑实测 4172 ≠ 方案预期 4171

> 日期：2026-09-01
> 执行 Agent：pf-agent
> 验收方：k3
> 触发红线：执行计划红线④「实测与设计不符时**停线回报 k3**」+ 验收文档 §六.1「4171 预期不成立则停线」
> 关联方案：《职业法术模块重整方案_20260826.md》§5 / 执行计划 阶段二

---

## 一、停线触发

KN136 修复已落地并完成真重跑。**实测 chunk 总数 = 4172，与方案/验收文档预期 4171 不符**，按红线④停线，请 k3 裁定后再推进后续步骤。

## 二、已完成工作（commit `c852987`）

1. **KN136 修复**：`formats/spell.py:1556` 后新增 `if new_name.startswith('[PZO'): continue` 拒收守卫（与空标题/CJK 守卫并列，拍板修法，未换修法）。
2. **TDD 先行失败测试**：新增 `TestKn136PzoTitleGuard` 2 用例（拒收 `[PZO` 伪标题 + 相邻真法术完整保留）。守卫缺位实测红（`[PZO9202 神与魔法（Gods and Magic）` 被切为伪标题），守卫在位实测绿。
3. **真重跑**：`vectorizer.pipeline --category spell` → link 步骤 → 实测数字见 §三。

## 三、实测数据（重跑后）

| 项 | 方案预期 | **实测** | 说明 |
|---|---|---|---|
| chunks.jsonl 行数 | 4171 | **4172** | pipeline 产出，link 后不变 |
| index.json total_chunks | 应与行数一致 | **4172** | pipeline 重跑自动写，残留 4173 已消除 |
| `[PZO` 开头 title | 0 | **0** ✅ | 修复生效 |
| page_1363 chunk | 140 | **140**（0000~0139，id 连续无断裂） | 无伪标题 |
| KN011 link | 100% | **257/257 = 100%** | 与旧基线一致 |
| verify_spells | 全过 | **全过** ✅ | 学派/来源/字段/无污染/无重复 |
| verify_kn011_018 | — | 26/32 通过 | 6 项 KN013 既有偏移复现 + G 不变量(4200)待调整（见 §四.3） |

**「三者一致」基础成立**：chunks.jsonl 4172 == index.json 4172（pytest 基线断言 4172 数字不变，见 §五）。

## 四、与方案预期的偏差分析

### 4.1 为何是 4172 而非 4171（核心）

- `git diff dd2abe6..HEAD`（4172 基线 commit → 当前 HEAD）：**spell 相关文件（formats/spell.py / processors/spell.py）零改动**；共享层仅两处：`formats/base.py` KN131 `_strip_http_links` 加粗链接行收窄（+1 [PZO 伪标题 bug）+ `processors/base.py` 新增 `entity_name_resolvers`/`pre_normalize` 钩子（默认空列表/原样返回，spell 零行为变化）。
- KN136 修复（-1）恰好抵消 KN131 收窄（+1），**净 0** → 4172。
- **KN136 原文档（2026-08-05，登记）明确：「修后红线恢复 4172」**——与实测一致，方案「预期 4171」与之矛盾，判断为方案作者的基线误判（可能误以为当前 4172 产物含 bug）。
- page_1363 实测 140 chunk（0000~0139，id 连续）与 KN136 文档「128 个 chunk id 回到原编号」目标吻合。

### 4.2 重跑发现链接行残留（新现象，需裁定）

`page_1363_0011 创造万物` text 尾部比旧基线**多一行**：
`**[PZO9202 神与魔法（Gods and Magic）](http://www.goddessfantasy.net/...)**`

- 根因：KN131 收窄后 `_strip_url_lines` 不再剥除「锚=中文正文」的加粗链接行；KN136 修复只**拒收**（不切伪标题）不**剥除**，该行并入相邻 chunk text 尾部。
- 旧基线（dd2abe6）：该行被整行剥除，text 干净。
- 影响：1 个 chunk 的 text 尾部多一行书目导航链接。检索/过滤无实质影响，但与旧基线内容不完全一致。

### 4.3 既有偏移（非本批次引入）

- **book 14 条**：水下冒险AA 9 条 `?`→`AqA`、海洋之血BotS 5 条 `CAMPAIGN`→`BotS`——来源解析 provider 链 dd2abe6 后演化，非 KN136 修复引入。
- **verify_kn011_018 KN013 6 项失败**：duration 含句号 28 / len>40 21、target len>40 16、effect len>40 28、M1 residue 119——与 2026-08-01 登记的既有偏移同家族（旧值 26/19/16/28/109，M1 旧 109），属方案第 9 步「KN013 复测复现则保持登记」范畴。

## 五、待 k3 裁定事项

1. **新基线采用 4172**（与 KN136 原文档一致、实测权威）？若是，后续按 4172 推进——`verify_kn011_018.py:125` G 不变量 4200→4172、`test_chunk_count` 断言 4172 数字不变但补 page_1363 针对性断言（140 无伪标题）、cicd 接入 spell。若 k3 坚持调查 4171 依据，请指示。
2. **链接行残留处理**：接受残留（当前状态，text 尾部一行导航链接，检索无影响）？还是在 normalize 层剥除（需换修法，属红线④「不得自行换修法」范畴，须 k3 拍板）？
3. **KN011 link 内建方式**（spell 接入 cicd 的必要前置）：spell 当前 link 是独立脚本 `link_mythic_base_spells.py`，cicd 四步链 pipeline 步骤不含 link → 产出的 chunks 缺 `base_spell_*` 字段 → verify_kn011_018 KN011 指标 FAIL。请裁定：
   - **方案 A（推荐）**：将 link 内建为 `SpellProcessor.POSTPROCESSORS` finalize 组件（对齐 feat 决策 A：pipeline 单命令产出含 link 的最终态，link 不删行 → index.json 与 chunks 行数天然一致）。改动面：`processors/spell.py`（声明 POSTPROCESSORS）+ 新增 `postprocess/spell_chain.py`（从 link 脚本重构）+ link 脚本保留兼容。
   - **方案 B**：cicd `run_chain` 对 spell 特判，pipeline 后追加 link 子步骤（动共享编排逻辑，污染通用链路）。

## 六、裁定后继续的步骤（待 k3 确认后执行）

1. `verify_kn011_018.py` G 不变量按 4172 调整（附实测依据）
2. `test_chunk_count` 补 page_1363 针对性断言（140 chunk / 无 [PZO）
3. link 内建（方案 A/B 按 k3 裁定）→ `cicd/verify.py` CATEGORIES 补 spell → 四步链全绿
4. 共享层回归（若 link 内建未触 base.py/pipeline.py 共享层则 git 实证豁免）
5. Java 侧零改动 → 重建 `~/.pf_agent/vector_store_spells.json` → evaluate.py 50/50 · ≥2.96
6. KN136 冻结令解除登记（标注解除日期与本批次）+ KN013/018 复测结论
7. 交付文档 + 独立提交，交 k3 验收

## 七、回滚保障

- 旧基线已备份：`vectorizer/output/法术/chunks.jsonl.pre-stage2-20260901`（4172 旧产物）+ index.json.pre-stage2-20260901
- 重跑产物 gitignored，未入库；代码改动仅 commit `c852987`（可 revert）
