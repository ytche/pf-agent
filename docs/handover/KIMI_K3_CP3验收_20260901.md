# KIMI_K3 验收文档 · CP3 pytest 收敛 + cicd 接入 + 真重跑

> 批次：CP3 · 阶段一
> 日期：2026-09-01
> 验收人：k3
> 关联交付：《KIMI_K3_CP3批次交付_20260901.md》（commit 846e6ad）
> 关联方案：《职业法术模块重整方案_20260826.md》§4 CP3、V1-1
> 结论：**验收通过**

---

## 一、验收结论

CP3 **验收通过**，允许进入 CP4（Java 侧迁移）。职业模块自此进入 cicd 统一验证链，与 feat/race 等已验收类目同等待遇——本批次的核心目标达成。四步链由 k3 亲跑复现全绿，产物与 CP2 验收基线**逐字节一致**。

## 二、k3 独立验证记录（全部亲跑/亲读，非转述）

| 验证项 | 方法 | 交付声称 | k3 实测 | 结果 |
|---|---|---|---|---|
| 四步链全绿 | 亲跑 `python3 -m vectorizer.cicd verify --category profession` 完整链 | 全绿 | **✅ profession 完整验证链通过**（pytest → pipeline → verify 8 检查 → 不变量闸口） | ✓ |
| 产物零行为变化 | md5 对比 CP3 重跑产物 vs CP2 验收基线（k3 自留） | 逐字节等价 | **md5 完全相同**（b8408870…） | ✓ |
| 职业专属单测 | 亲跑 5 文件 | 44 用例 | **44 passed**（format 5 + heading 5 + verify_checks 14 + kn222 16 + postprocess 4） | ✓ |
| 不变量闸口 | 亲跑结果 + 阈值注释审查 | 按 13922 实测设定 | 实测口径逐条在注释在案；`--max-book-unknown 0` 顶格不预放符合棘轮设计 | ✓ |
| 索引等价性 | 新老 class_archetype_index.json 对照 | 290 类 / 1060 变体（老 287/1060） | **290/1060 vs 287/1060** ✓；圣骑士 45/45、女巫 40/40、吟游诗人 68/68 抽样一致 | ✓ |
| index.json 一致性 | 读产物 | — | total_chunks 13922 == 实际行数（无 spell 4173 式残留） | ✓ |
| 收敛去向 | 读脚本头注释 + _archive | 8 SUPERSEDED + 1 归档 + 1 pytest 化 | 逐一在案；保留 3 个非职业脚本未动（data_invariants/phase3/spell_list_recall） | ✓ |
| 圣骑士豁免证据 | 查源数据 | 3 项历史 bug 有源数据证据 | 灰骑士 page_55.md:1845 `<!-- UI-source:... -->` ✓；亡灵制裁者在 APG 表格分组（跨行单元格）✓ | ✓ |
| 豁免机制 | 读 verify_profession.py:655-680 | WARNING 降级非断言放宽 | drift → WARNING 且打印理由；新增漂移仍 FAIL，闸口未削弱 | ✓ |
| KN224 登记 | 读已知问题记录.md:6129 | 3 伪 class 登记不修 | 登记完整（成因/影响面/定性清楚） | ✓ |

## 三、设计审查要点

1. **CATEGORIES 装配**（cicd/verify.py:154+）：纯增量，race 显式传参模式，每个阈值带实测注释——正是方案要求的做法。
2. **ProfessionIndexWriter 走 finalize 扩展点**（postprocess/profession_index.py）：只读 chunks.jsonl 重建索引、不改 chunk、幂等——满足「CP3 不引入行为变化」的硬约束，且避免了复活老脚本 `write_outputs` 死路径。死代码 `build_class_index` 如实披露留待清理，可接受。
3. **基线修订合规**：`_PALADIN_SOURCE_DRIFT` 是「豁免登记 + 证据 + KN 记录」，不是放宽断言；冬女巫 ISM/UM 双来源断言修正有源数据支撑。红线 ③ 遵守良好。

## 四、遗留事项（不阻塞 CP4）

1. **KN224**（3 伪 class / 5 chunk）：CP2 基线固有，登记不修，另立任务。仅污染审计索引类目数（290 vs 287），不影响检索。
2. **`build_class_index` 死代码**：CP4 之后清理或随老脚本归档一并处理，k3 建议在老脚本退役时一并清。
3. 老脚本 `vectorization_prep_profession.py` 归档/标 superseded 在 CP4 完成 Java 切换后执行（届时 Java 不再读老产物路径）。

## 五、流程备注

- k3 的 CP2 验收文档与状态更新被并入执行方的 CP3 commit（846e6ad）入库——内容无损、来源清晰，下不为例：验收产物由 k3 自行提交，执行方 commit 只含自己的工作。

## 六、下一步（CP4 要点提醒）

按方案 §4 CP4 执行，k3 验收时将重点亲验：

1. **删除**：ClassChunk.java / ClassChunkMapper.java / ClassChunkMapperTest.java；
2. **新增**：ProfessionChunkMapper（三字段扁平化单测必须含 null 变体——约半数 chunk 无 archetype_name/feature_subtype）；
3. **改造**：ClassDataLoader 读 StandardChunk；`application.yml:31` classes-path 切到 `vectorizer/output/职业/chunks.jsonl`；
4. **V1-5b 硬项**：重建向量库后从库中抽 ≥20 条 class Document，确认 metadata 含 `class_name`/`archetype_name`/`feature_subtype` 扁平键且取值正确（防 mapper 漏键静默失效）；
5. **评测回归**：evaluate.py 全量 50 题，溯源 50/50、结构 ≥2.96；
6. `mvn test` 全绿（基线 166，删除 ClassChunkMapperTest 后净测试数只增不减）；
7. 过滤层零改动（FilterTranslator 不动）——若执行中发现必须改，停线回报。

CP4 完成后阶段一收口，再开阶段二（spell KN136 解冻）。
