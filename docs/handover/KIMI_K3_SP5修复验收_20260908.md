# KIMI_K3 验收文档 · SP5 修复轮（embedding 双通道）

> 批次：SP5 修复轮（承接首轮打回）
> 日期：2026-09-08
> 验收人：k3
> 关联：《KIMI_K3_SP5验收_20260908.md》（首轮不通过 + 打回清单 R1~R3）；《KIMI_K3_SP5修复交付_20260908.md》（修复 commit `1c174f4` + 交付文档 `fe249d6`）
> 结论：**验收通过**（R1~R3 全项 k3 亲验复测；合并待用户拍板）

---

## 一、验收结论

修复轮**通过**。首轮 P0（`dimensions()` 无兜底被 Spring AI 观测链路击穿、fallback 全失效）已修复，k3 按**比交付更严的冷启动场景**亲测通过：启动探测后立即 kill stub（dimensions 缓存未建立、零查询）→ 首问即触发 `dimensions 失败，降级 ollama` WARN（正是首轮炸掉的位置，现被 withFallback 接住）→ 缓存 1024 → embed 降级 → **15 条真实 ollama 检索 + 正确答案**（火球术 塑能系[火]/标准动作，gap=false）。

## 二、k3 复验记录（全部亲跑/亲读）

| 项 | 方法 | k3 实测 | 结果 |
|---|---|---|---|
| R1 代码 | 亲读 `1c174f4` diff | dimensions() 纳入 withFallback + `volatile int cachedDimensions`（0 哨兵）；并发注释严谨（多线程首并发各解析一次无害，维度恒定 1024）；修复 diff 仅 2 文件 +65/−5 | ✓ |
| R1 单测 | 亲跑 `mvn test` | **247 全绿**（244+3：降级/缓存 times(1)/双挂上抛） | ✓ |
| R2 V5③' 冷启动 | 自建实例（cloud 主 + fallback=true + stub :9999），启动探测后**立即 kill stub** 发问 | 第一条 WARN 即 `cloud 调用 dimensions 失败，降级 ollama`；随后 embed 降级 ×2；sources=15 真实 ollama、答案正确、gap=false | ✓ **P0 消除** |
| R2 V5①② | 同实例启动 + stub 恢复后再发问 | ① 启动「维度探测」落 stub；② stub 恢复后下一问 60 条请求全部落 stub——**逐次降级语义成立，主恢复自动回主** | ✓ |
| R2 V5④ | 不注入 key 启动 cloud 主 | fail-fast 报 `请设置环境变量 PF_EMBEDDING_CLOUD_API_KEY` | ✓ |
| R2 V2 | 默认参数启动 | `primary=ollama（默认单通道，fallback 未启用）`；`cloud\|维度探测` grep 0 命中 | ✓ |
| R3 文档 | 亲读修复交付 | 首版 V5③ 声称矛盾如实披露、根因复述准确、修复前后对照表清晰 | ✓ |

## 三、观察项（不阻塞）

1. **cloud 主通道的调用量**：V5② 一单问 stub 记录 60 条 embed 请求（8 store × 多轮 vectorSearch）。dimensions 缓存已消除重复探测，但「每 store 每条查询一次 embed」是 SimpleVectorStore 固有形态——**托管部署选 cloud 主时需留意 embedding API 调用成本**，登记供 SP6 部署文档/成本估算引用。
2. **yml 显式 `fallback-enabled: false` 覆盖 D6 默认**（首轮观察项 1 维持）：「cloud 主默认兜底」需删该行或参数/env 覆写，SP6 部署文档必须写明。
3. 首轮验收文档（`KIMI_K3_SP5验收_20260908.md`）作为打回留痕保留，不删不改。
4. 真实硅基流动 key 的一致性抽查仍挂起（无 key；机制已由 stub 全覆盖，风险为浮点级数值差异，方案 §5 已登记）。

## 四、SP5 总账与下一步

- 方案 V1~V6 全项通过（首轮 V5③ 缺陷修复后复验通过）；embedding 双通道闭环：默认 ollama 零变化，cloud 主/备 + 维度自检 + 逐次降级 + 失败 fail-fast。
- 分支 `feat/sp5-embedding-dual`（T1~T3 + R1 + 文档，共 6 commit）**可合并**，由用户拍板。
- 下一子项目：**SP6 开源化工程**（README/LICENSE/配置外置/Docker/CI/issue 模板——决策记录注明需用户拍板范围，k3 将先出方案）。
